{
  "comment": "Single source of truth for inventory query tools exposed through MCP and backend-only display tools exposed to Bedrock. Backend schemas cover all 11 tools; the MCP server registers only the 5 query tools.",
  "tools": [
    {
      "name": "search_inventory",
      "properties": ["name", "set_id", "condition", "min_value", "max_value", "language"],
      "required": []
    },
    {
      "name": "get_inventory_summary",
      "properties": [],
      "required": []
    },
    {
      "name": "get_card_price_history",
      "properties": ["card_id"],
      "required": ["card_id"]
    },
    {
      "name": "calculate_inventory_value",
      "properties": [],
      "required": []
    },
    {
      "name": "flag_underpriced_cards",
      "properties": ["threshold"],
      "required": ["threshold"]
    },
    {
      "name": "display_card",
      "properties": ["item_id"],
      "required": ["item_id"]
    },
    {
      "name": "open_display_panel",
      "properties": [],
      "required": []
    },
    {
      "name": "close_display_panel",
      "properties": [],
      "required": []
    },
    {
      "name": "add_to_display",
      "properties": ["item_id"],
      "required": ["item_id"]
    },
    {
      "name": "remove_from_display",
      "properties": ["item_id"],
      "required": ["item_id"]
    },
    {
      "name": "reorder_display",
      "properties": ["item_ids"],
      "required": ["item_ids"]
    }
  ]
}
