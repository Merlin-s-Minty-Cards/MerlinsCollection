# Git worktrees (local parallel-task scratch space)
.worktrees/

# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# OpenNext build output (RFC 0014 Task 6) — regenerated by
# `npm run build:opennext`, deployed by cdk-nextjs-standalone; not source
.open-next/

# Environment files — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# TypeScript build output
dist/
*.tsbuildinfo

# Coverage
coverage/
.nyc_output/
lcov.info

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Sanity
.sanity/

# Superpowers brainstorming companion (mockups, server state)
.superpowers/

# Personal learning notes (not project docs)
docs/learning/

# Agent workflow progress/roadmap scaffolding (not project docs)
claude-progress.txt

# Claude AI agent runtime artifacts (reviews, temp state)
.claude/council/
.claude/scheduled_tasks.lock

# Business spreadsheet exports (private financial data — never commit)
data/spreadsheet/

# Review-triage artifacts (contain cost bases / internal notes — never commit)
review*.html
backend/review*.html

# Redeploy payloads contain ECS env incl. secrets — never commit
deploy/.redeploy/

# CDK synth output (infra/) — regenerated by `cdk synth`/`cdk deploy`
cdk.out/

# Local scratchpad (Artifact drafts, throwaway working files) — not project docs
.scratch/
