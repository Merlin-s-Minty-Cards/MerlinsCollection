import { describe, it } from "vitest";

describe("calculateInventoryValue", () => {
  it.todo("calculates total value across all cards");
  it.todo("breaks down value by set");
  it.todo("breaks down value by condition");
  it.todo("includes a timestamp on the result");
  it.todo("returns zero for an empty inventory");
});
