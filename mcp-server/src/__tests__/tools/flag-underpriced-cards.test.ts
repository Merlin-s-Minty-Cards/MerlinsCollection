import { describe, it } from "vitest";

describe("flagUnderpricedCards", () => {
  it.todo("flags cards listed below the threshold percentage of market price");
  it.todo("does not flag cards at or above market price");
  it.todo("includes discount percentage on each flagged card");
  it.todo("returns empty array when no cards are underpriced");
  it.todo("respects a custom threshold percentage");
});
