import { describe, it } from "vitest";

describe("getCardPriceHistory", () => {
  it.todo("returns price history for a known card id");
  it.todo("returns history sorted chronologically");
  it.todo("throws when card id does not exist");
  it.todo("includes data source on each price point");
});
