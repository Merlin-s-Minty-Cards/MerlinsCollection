# RFC 0010 — Follow-ups

Out-of-scope findings. **Append here rather than fixing them as a side errand.** One row
per finding, filed under the task that found it, with enough context that a later reader
can decide whether it still matters.

The rows already present were found **during planning**, by reading the code the twelve
reports pointed at. They are real and unfixed; none of them blocks a task.

## Found during planning (2026-08-10)

| Task | Finding | Why it was not fixed here |
|---|---|---|
| T0 | **Prep Queue and Show Prep parse one value and send another.** Both guard on `parseFloat(stickerValue)` then `PUT { sticker_price: stickerValue }` — the raw string (`outgoing/page.tsx:140-150`, `show-prep/page.tsx:133-138`). Harmless today only because the input is `type="number"`, so the string is already numeric-clean | It becomes a live bug the moment T1 replaces those inputs. **T1 must fix it in the same change** — this row exists so T1 does not miss it |
| T0 | **`InlineEditCell` takes `type: 'number' \| 'url'` and no money variant** (`components/admin/shared/InlineEditCell.tsx:9-10`). Every inline price edit in the admin goes through it | T1's rollout has to decide whether `MoneyInput` composes into `InlineEditCell` or replaces it for money. A shared component's signature change is its own piece of work |
| T2 | **`put_payout` keys on the partner's NAME** — `PAYOUT#{payout.partner}#{payout.payout_id}` (`services/dynamodb.py:1317`). Renaming a partner forks the row exactly as consignors fork today, and `put_debt` shares the shape | No UI edits payouts, so nothing can trigger it yet. It is the same latent class T2 fixes for consignors; worth sweeping when a Payouts tab is built |
| T2 | **`get_consignor` and `delete_consignor` linear-scan the whole `CONSIGNORLIST` partition and act on the FIRST match** (`dynamodb.py:1376-1391`). With the fork fixed there is only ever one match, so this is correct but fragile — it encodes "there is exactly one row per id" without asserting it | T2 removes the condition that makes it wrong. Making it assert instead of assume is a robustness change with no current failure |
| T3 | **`GET /admin/triage/counts` does a full `list_inventory()` on every admin page render** (`routers/admin/triage.py:48`, `AdminShell.tsx:61-68`). Deliberate — a cached badge that still counts a fixed card is the failure the badge exists to prevent | Cheap at hundreds of items. RFC 0008's follow-ups already flag it; revisit at ~10k items, and if a cache ever lands, the invalidation has to include T3's bulk clear |
| T3 | **`review_reason` is one column shared by machine keys and human free text** (`models/inventory.py:170-230`), so "is this a machine flag" is a set-membership test rather than a field | Accepted seam from RFC 0008 T11. T3's bulk clear depends on that membership test being exact — if a human ever types the literal string `blank_condition` as a note, their flag is clearable in bulk. Vanishingly unlikely; recorded because it is the one way the guard fails |
| T5 | **The Triage page's own optimistic updates recompute reasons locally** (`triage/page.tsx:101, 264, 281`) even after T3 makes the server authoritative | Correct for an optimistic update — there is no server answer yet. But it means the two rule sets still both exist, just with the client's copy demoted to "predict the next state". If they drift, a row will briefly show the wrong chip and then correct itself |
| T5 | **`CardDetailModal` is mounted by only six pages.** Buy, Trade, Market, History, Cosigners and `/admin/card/[id]` have their own detail surface or none, so "Send to Triage" is still unreachable from them | Pre-existing gap, already filed as RFC 0008 follow-ups T5 row 1. T5 does not widen it, but every fix landing in this modal keeps missing those pages |
| T6 | **`/admin/card/[id]` duplicates the modal's field-rendering layout** rather than sharing it | T6 fixes the modal. If the card detail page has the same cramped-column problem it needs the same fix, and the honest answer is that the two should share a component — a refactor, not a bug fix |
| T8 | **`SlabList` computes a relative age with `new Date(iso).getTime()`** (`components/admin/slabs/SlabList.tsx:32`). That value is a full timestamp, not a date-only string, so it is unambiguous and correct | Listed so a later reader does not "fix" it into a bug. T8's ban is on `new Date()` for **date-only** values |
| T9 | **`Expense.amount` is already signed** — negative means money came in, matching the source spreadsheet (`models/business.py:62-66`) — while `Transaction.amount` is unsigned with direction in `type` | Two conventions in one ledger. T9 renders transactions signed for display, which makes them *look* like expenses without *being* stored like them. Unifying storage is a migration, and inverting signs would silently change every existing aggregate |
| T11 | **Show analytics snapshots are stored point-in-time records, not derived views** (`put_show_analytics`, `dynamodb.py:1277`). A void leaves them stale by construction | T11 marks them stale and offers regeneration. Making them derived would remove the whole problem class and is a genuinely larger change to how show reporting works |
| T11 | **`_apply_review_transition`'s rule 2 (automation must not re-flag a reviewed item) has no automated writer to guard against yet** (`admin/inventory.py:1121-1124`) | Pre-existing note from RFC 0008. Relevant here only because T3's bulk clear and T11's void both write `needs_review`-adjacent state, and a future re-matcher would interact with both |
| T12 | **`attach_price` re-verifies the join on the cached `price_source_id` path but nothing clears `price_source_id` when an admin re-points `card_id`** (`services/slab/pricing.py:451-470`, and its own comment says so) | The re-verify makes it safe — a mismatched pair refuses rather than mis-prices. The stronger fix belongs in the Triage re-point flow, which T3 touches but for a different reason |
| T12 | **The quota counter is per-process and in-memory** (`services/slab/quota.py`; owner-adjacent decision 2026-08-09). T12 adds a second caller — intake — to a counter that assumed one scheduled process | Still self-correcting: it re-reads `x-ratelimit-daily-remaining` on every successful call and an overshoot is a 429 the loop stops cleanly on. But with two ECS tasks and an intake-time trigger, "one process" is no longer the right mental model. **Revisit if the run summary ever reports credits it did not spend** |
| T13 | **The mobile bottom nav takes `navItems.slice(0, 5)`** (`AdminShell.tsx:166`) | T13 replaces it with an explicit array. Filed because the *reason* matters: flattening a nested structure and slicing it silently changes what a phone shows, and nobody would notice |
| T2 | **`DELETE /admin/inventory/{item_id}`'s soft mode sets `ItemStatus.LOST`** (`admin/inventory.py:392-415`), which is a real lifecycle state — a physically lost card — not an archive flag. So "I added this by mistake" and "I cannot find this card" become indistinguishable, and a mistaken row stays in every status-based query as LOST | It is a third pattern alongside Shows/Consignors (archive) and Locations (hard delete + 409), and CLAUDE.md now says so. Making inventory items archivable properly means deciding how `archived` interacts with `status`, the customer-visibility gate, analytics and lineage — a design pass, not a rename |
| T16 | **Creating local catalog rows for cards TCGdex does not carry.** T16 hand-values them instead | A real feature — identity, images, set membership, how the sync treats a row it did not create, what happens if TCGdex later publishes the same card. If it is ever built it **supersedes** T16 rather than extending it |
| T16 | **An unlinked graded slab cannot store a graded price at all.** `GRADEDPRICE#` rows are keyed `CARD#<card_id>`, which is why `PUT /admin/slabs/{id}/price/pin` 404s for one (`routers/admin/slabs.py:360-395`) | T16 routes these to the item's own `current_market_value`, which works. Giving graded prices a card-less home would be a key-layout change to a schema RFC 0009 deliberately did not touch |
| T15 | **`/admin/card/[id]` and the inventory search results were not audited** for the name-alone rule — only the five catalog *pickers* were | The rule in CLAUDE.md covers "every surface where a human picks a card out of a list of candidates". A detail page is not a picker, but inventory search results arguably are. Worth a sweep once `CardPickerRow` exists |
| — | ~~**`ChatPanel.test.tsx` fails 6–7 tests under full-suite parallel load and passes 12/12 alone.**~~ **FIXED 2026-08-11, and it was never flakiness** — `vi.clearAllMocks()` leaves the `mockResolvedValueOnce` queue intact, so one test timing out (3317 ms of a 5000 ms budget, idle) fed its leftover replies to four neighbours | Kept, struck through, because the *reasoning* was the problem: it passed 12/12 alone, so three tasks in a row concluded "environmental" and moved on. The wobbling failure count was the evidence that one failure caused the rest. See progress.md's baseline section |

## Found during execution

*(append below — task id, finding, why it was not fixed there)*

| Task | Finding | Why it was not fixed there |
|---|---|---|
| T0 | **The backend accepts a NEGATIVE `buy_price`.** `_coerce_decimal` checks presence and finiteness, not sign, so `POST /purchases/{id}/items {"buy_price": -5}` is a 200 and confirms into a negative `cost_basis`. `parseMoney` rejects `-5` on the frontend, so no admin UI can produce one — but MCP and curl can | T0's scope named "absent, `None`, or not finite" explicitly. Adding a sign rule is a one-line change but it is a **contract** decision (is a negative purchase ever legitimate — a refund? a correction?) that T11's void work is about to answer properly. Fixing it blind here would pre-empt that |
| T0 | **`confirm_buy_session` is now safe against a bad ROW, not against a failing WRITE.** Pass 1 builds and validates everything, pass 2 writes; if `put_transaction` throws on row 3 for an infrastructure reason (throttling, a dropped connection), rows 1–2 are still written with no rollback | Genuinely different failure class, and out of T0's scope. The doc explicitly bans adding rollback here. The real fix is a DynamoDB `TransactWriteItems` batch, which is a repo-layer change bounded at 100 items — worth raising when T11 gives the ledger a correction path |
| T0 | **`sales.py` and `trades.py` have the same single-pass build-and-write shape.** T0 only fixed `purchases.py`, because that is where the reported bug was and where the slab commit runs | Same partial-write class, different router. They are not currently reachable with a malformed amount from their own UIs (all `type="number"`), but **T1 changes those inputs**, so T1 should either widen `_build_purchase`'s pattern to them or file it as its own task. Do not assume T0 covered them |
| T0 | **`formatMoneyInput` returns exponential notation above 1e21**, because `Number.prototype.toFixed` does. The field would then show `1e+21` and `MoneyInput` would immediately flag it unreadable | Self-consistent and loud rather than silently wrong, and no card costs $10^21. Recorded so nobody "fixes" the exponent rejection in `parseMoney` without noticing it is what makes this degrade safely |
| T1 | **Sell's per-item agreed price and bulk discount NEVER REACH THE API.** `updateItemPrice` (`sell/page.tsx:181-185`) and `applyBulkDiscount` mutate local state only; `handleConfirm` PATCHes session metadata and POSTs `/sales/{id}/confirm`, and no request in between carries the edited `agreed_price`. The price the sale records is whatever `addItem` posted when the card was added — sticker, else market. **So discounting a card at a show is editing a number that is thrown away** | Found while writing T1's "typing 1,300 sends 1300" test for Sell and discovering there is no send to assert on (the test asserts the displayed total instead). It is not a parsing defect, so fixing it is a scope change, not a side errand: it needs an item-update endpoint or a per-item body on confirm, plus a decision about what a consigned item's `minimum_price` does to a discount. **Verify this against the live site before building anything** — it is the largest thing T1 turned up and it costs real money |
| T1 | **Inventory, Shows and History still have native `type="number"` money inputs** — `inventory/page.tsx:750,754` (cost basis, market value), `shows/page.tsx:356,367` (sales goal, cash at start), `history/page.tsx:286,298,310,321` (min/max price and profit filters) | None appear in the task doc's measured table, and none is the show-floor path the owner reported. They are the same class though: each will swallow a comma until converted. The History four are client-side filters and the Shows two are configuration, so the exposure is smaller than Buy/Sell/Trade — but "smaller" is not "none", and the rollout is not complete until they are done |
| T1 | **`sales.py` and `trades.py` still have the single-pass build-and-write shape** that T0 fixed in `purchases.py` — carried forward from T0's own follow-up row, which asked T1 to widen it or file it | T1 is frontend-only and its doc says *"Do not change any API contract"*, so widening `_build_purchase`'s two-pass pattern into two more routers does not belong here. It is now more reachable than it was, not less: those UIs are text inputs, so a malformed amount can arrive from a browser rather than only from curl. Pairs naturally with **T11**, which is giving the ledger a correction path anyway |
| T1 | **Percent fields were left as `type="number"`, deliberately** — `buy_pct`, `payout_percent`, `split_percent`, Sell's bulk discount, Show Prep's threshold slider | The task doc recommended this and it is right: percents are bounded 0–100 and cannot carry a thousands separator, and running `parseMoney`'s currency-symbol stripping over one would make `$50%` parse. Recorded so a later sweep does not "finish the job" by converting them |
| T1 | **`InlineEditCell`'s money mode saves a formatting-only edit when the stored value has trailing zeros** — stored `'25.00'`, typed `'25'`, committed `'25'`, which differs as a string so the dirty-check does not skip it | One redundant PUT with an identical value. Fixing it means comparing numerically rather than by string, which the `type="url"` and `type="number"` arms do not do either — a consistency change to the whole component, not a money one |
| T15 | **`/admin/market` crashes the whole page if `GET /market/card/{id}/trend` returns 200 with no `points` key.** `loadPriceHistory` does `setPriceHistory(res.points)` unguarded (`market/page.tsx:293`), and the render then reads `priceHistory.length` — `TypeError: Cannot read properties of undefined`. The `catch` arm sets `[]` correctly, so only a malformed **success** reaches it | Found because T15's new test is the first one that ever clicks a result row; the crash surfaced as an unhandled error with a `{}` mock. The real endpoint always sends `points`, so this is hardening, not a live bug — and the fix (`res.points ?? []`) belongs with a sweep of the same shape elsewhere on the page (`confidence` has the identical pattern), not bolted onto a display change. The T15 test now mocks the realistic shape |
| T15 | **`PriceDisplay` parses with `parseFloat` and renders `—` for anything unparseable** (`components/admin/shared/PriceDisplay.tsx:15`). Every money display in the admin goes through it | Safe for `display_price`, which is a server-generated decimal string with no grouping. But it is the one remaining `parseFloat` on a money path after T1 removed the rest, and if a comma-grouped string is ever passed to it, `parseFloat('1,300')` renders **$1.00** rather than failing. `CardPickerRow` deliberately does not pre-format around it |
| T15 | **`display_price`/`display_finish` were added to `GET /admin/market/search` only.** `GET /admin/market/card/{card_id}` still returns the bare `model_dump`, so a caller reading one card has to pick a finish itself | No caller does today — the detail panel reads `prices` directly. Recorded because the next one will, and the whole point of T15's backend half is that finish selection has exactly one implementation. `_with_display_price` is already a reusable one-liner if that endpoint ever needs it |
| T15 | **The five pages still declare five near-identical `CatalogCard` interfaces**, now each extending `PickerCard` rather than restating its fields | Better than five copies, but the search *response* type is still per-page and none of them is derived from the backend model. A generated or shared `CatalogSearchResult` type would make a backend field addition visible at every call site instead of silently absent. Out of scope for a display change |
| T15 | **Buy's `selectCatalogCard` still walks `card.prices` by hand** to pre-fill `market_value` (`buy/page.tsx:133-140`) — it takes the first finish carrying a `market`, with no fallback order | This is the fifth copy the task doc warned about, and it predates T15. It is now redundant: the row already renders a server-chosen `display_price` computed by `_market_price`'s real walk, so the form could simply take that. Not changed here because it feeds `buy_pct` and the buy-price recalculation, i.e. it is a **money path**, and swapping the figure it prefills is a behaviour change that deserves its own test |
| T17 | **PRODUCTION DOES NOT RUN `daily_sync.py`, so its exit-code protocol reaches nothing.** The EventBridge schedule runs `python -m scripts.scheduled_sync --job prices` (`deploy/scheduled-sync-eventbridge.json:38`, `docs/aws-setup.md:490`), and `scheduled_sync.main` prints a JSON summary and returns **0 unconditionally** — `aborted`, `catalog_aborted` and both lock-skips all exit clean. The feature itself is fine (`run_daily_sync` is what the schedule calls, so the catalog cycle really does run nightly and its counts really do land in the CloudWatch JSON line); it is only the **signal** that is missing | T17's own RED gate named `daily_sync.py` and its exit codes, so that is what was built — and it is still the right thing for a hand-run. But the gap is now inherited by a second unattended pass, which is exactly the "a week of half-runs looks like a week of clean ones" failure the depth pass's exit codes were written to prevent. **The fix is ~5 lines in `scheduled_sync.py`** (read `aborted`/`catalog_aborted`/`skipped`/`catalog_skipped` off `result` and return non-zero, with `status: "degraded"` in the JSON). Filed rather than done because changing what an ECS scheduled task returns is an ops-visible behaviour change — it can start failing an alarm nobody has set up yet — and that is the owner's call |
| T17 | **A card TCGdex has permanently retired is re-fetched every single night, forever.** A 404 writes nothing, so `last_synced_at` never moves, so the card stays at the head of the stalest-first queue. Correct and self-healing for a *transient* 404; pure waste for a permanent one | Bounded and cheap today — TCGdex retirements are rare and each costs 262 ms — so the queue-head cost is a rounding error against 5,500 cards. It becomes real only if a large set is withdrawn upstream, at which point those ids would eat the front of every night's budget. The fix (stamp a "checked, still gone" marker so a 404 ages like a success) means writing to a row TCGdex says does not exist, which is a schema decision, not a loop fix. **The one-time script already sidesteps it** by selecting once per run |
| T17 | **The cycle writes ~31,600 price points a week for cards nobody owns** — `_refresh_one_card` appends history on every success, and that is deliberate (it is the shared body, and a second variant that skips history is exactly the divergence the extraction exists to prevent) | ~1.6 M rows a year, ~$0.75/month of storage, and never scanned: price history is read per card out of the `CARD#<id>` partition. So it is cheap and arguably valuable — catalog-wide history is a thing this business has never had. Recorded because it is a real growth curve nobody has agreed to, and because the first person to notice the row count should find a decision here rather than a surprise |
| T17 | **`run_daily_sync` does not wrap the catalog pass in a try/except**, so a repo-level failure (not a per-card one — a DynamoDB outage during the candidate scan) propagates out of the whole job | It runs LAST, so the depth pass, both snapshots and the denormalization have all already committed their work by then — nothing is lost but the summary print and a clean exit. Matching `refresh_graded_prices`' posture exactly, which is also unwrapped at this level. Filed because "degrade alone" is stated as a rule in the task doc and this is the one path where it holds by ordering rather than by construction |
| T17 | **`GET /admin/market/coverage`'s two new numbers are as fresh as the process catalog cache**, since they are computed off `_scan_catalog` like the counts beside them | Correct for a coverage panel, whose whole job is a periodic snapshot, and the alternative is the 11-second full-table scan T9 removed from the request path. Worth knowing before anyone treats `catalog_cards_stale` as a real-time alarm signal — it is a dashboard number, not a monitor |
| T17 | **The manual check in the task doc was NOT performed** — running the nightly job against live data with `CATALOG_REFRESH_CARDS_PER_NIGHT=50`, confirming 50 `brief` rows come back `full`, and proving the script at `--limit 200` | Both write to the live `merlins-cards` table and spend real requests against a volunteer-run free API, which is the owner's call to make and not a side errand. The `--limit 200` proof is listed in progress.md's "Blocked / needs the owner" section alongside the full overnight run. Note the IAM prerequisite: the cycle's candidate scan needs `dynamodb:Scan`, the same permission gap CLAUDE.md records for catalog search |
| T2 | **`put_payout` and `put_debt` have the identical latent fork.** `PAYOUT#{payout.partner}#{payout.payout_id}` keys on the partner's NAME and goes through `_gen()` without a sweep (`services/dynamodb.py`), and `put_debt` shares the shape. Renaming a partner forks the row exactly as consignors forked | Explicitly out of scope per the task doc's "Do not" list, and still un-triggerable: no UI edits a payout or a debt, so nothing can create the fork yet. The fix is now a three-line copy of `put_consignor`'s sweep — worth doing **with** the Payouts tab, not before it, because it needs the same "is a rename a new entity or an edit?" answer the UI will settle |
| T2 | **`repo.delete_consignor` is now unambiguously dead code** — no route calls it, and after this task none ever will, because archive is the semantics the owner chose | Left in place because the task doc says so explicitly ("stays uncalled, as it is today") and deleting a public repo method is not a display-layer errand. Flagged so the next reader knows it is dead rather than a hard-delete path they might wire up by mistake — wiring it up would break the archiving contract |
| T2 | **The detail panel offers no archive/unarchive control** — both live only on the table row | Unchanged from before T2 (delete was row-only too), so not a regression. But the panel is where an admin lands after clicking a consignor, and it *does* render the status badge, so the action is one click away from where the state is shown. A small addition, but it needs the panel's "selected consignor was just archived" refresh path thought through, which is the same `onUpdated` signature work **T5** is doing for `CardDetailModal` |
| T2 | **A consignor's name is not required to be non-empty.** `POST /admin/cosigners` accepts `{"name": "   "}`, which the new `.strip()` stores as `""`; the duplicate guard then makes `""` collide with itself, so a second blank-named consignor 409s with `A cosigner named "" already exists` | Pre-existing (the old route stored the raw string), and the frontend already disables Create on a blank name, so only a direct API call reaches it. Not fixed here because a `min_length=1` on `Consignor.name` is a model-level validation change that would also reject legacy rows on read, which is exactly the migration trap `archived` was careful to avoid |
| T2 | **The manual check in the task doc was NOT performed** — editing the real imported Harry, reading the 409 text, and confirming the already-soft-deleted Harry renders as **Archived** | It needs the live table and the owner's own data; tests can pin the contract but not the row. Listed in progress.md's "Blocked / needs the owner" alongside the reconcile script it depends on — the check and the script are one sitting |
| T3 | **21 of the 27 live triage rows carry `needs_review = true` with NO `review_reason`, and nothing can ever bulk-clear them.** They pre-date the column, the data no longer distinguishes why they were flagged, and absence of a reason is deliberately not treated as a machine reason — so the queue's dominant cohort is hand-work, one **Clear review** click at a time | Not a defect: treating a bare flag as clearable is exactly the rule that would let a bulk button eat a human's flag, since a human "Send to Triage" with no note also stores `None`. Recorded because the bulk clear is the feature that *sounds* like it drains this queue and cannot. Listed in progress.md's "Blocked / needs the owner" so the size of the hand-work is known |
| T3 | **`bulk-clear-review` has the same partial-write shape T0 fixed in `confirm_buy_session`** — it writes row by row, so a repo failure on row 3 leaves rows 1–2 cleared, with no rollback and a 500 carrying no count | Genuinely different stakes: it mutates one boolean and a note, writes no money and no ledger row, and is **idempotent** — re-running the bulk clear finishes the job with no double-effect, because a cleared item stops being `bulk_clearable`. Filed anyway because it is the same class, and because the real fix for all of them is a repo-level `TransactWriteItems` batch (see T0's row above), not per-router rollback |
| T3 | **The confirm dialog's count is the count from the last fetch, and the UI never reports the `cleared` number the endpoint actually returns.** If the data moved underneath, the admin is told "Clear 6 flags" and may get 5 | The list refetches immediately after, so the outcome is visible — just not stated. Adding a result message is ~3 lines but needs a success-message surface this page does not have (it has `setError` only), and **T5** is already reworking how this page learns that something changed. Worth doing there rather than bolting a second notification pattern on here |
| T3 | **`include_terminal` is only honoured when `triage=true`**, because the scope filter lives inside that branch. `GET /admin/inventory/search?triage_reason=flagged` alone returns flagged items in every status | Deliberate and consistent: `triage_reason` is an ordinary AND filter like the three legacy ones, and only the *union* carries a scope. No caller does this — the Triage page always sends `triage=true`. Recorded because the asymmetry is invisible from the signature, and anyone building a second triage-shaped surface would have to know it |
| T3 | **The Triage page still has no pagination**, and the bulk clear's exactness depends on that: the confirm count is computed from the loaded rows, which are the whole filtered set only because the search returns everything | True today at 27 rows and fine at hundreds. It becomes a correctness bug the moment anyone paginates this endpoint — the button would then promise the count of one page while the endpoint clears the whole filter. Whoever adds pagination must move the count to a server-side preview |
| T4 | **A REASON FILTER that matches nothing still renders the success panel** — the same false-congratulation defect T4 fixed for the search. Filtering to `missing_english_name` (live count: **zero**) shows *"Nothing needs review / Every card is linked, named, and unflagged"*, which is a claim the filtered request never made. The split T4 added is keyed on `searchTerm` alone | Reachable today, and the fix is one line — `isEmpty && (searchTerm \|\| reasonFilter)` — plus its own copy ("No cards with that reason") and its own test. Not done here because the task doc names the *search* case as its one design decision, and the reason filter is **T3's** surface: widening T4 to cover it is the scope drift these task docs exist to prevent. Whoever takes it should generalise the panel to "no rows match the filters you have set" rather than adding a third branch |
| T4 | **Two in-flight queue fetches can resolve out of order, and the later-resolving one wins.** `fetchItems` has no cancellation and no sequence guard, so a slow request for `"Char"` landing after a fast one for `"Charizard"` leaves the wrong rows on screen with the right term in the box | Pre-existing — the reason `<select>` and the terminal checkbox could always do this — but T4 makes it *reachable*, because a search box is the first control on this page a human operates faster than the network. Mitigated in practice by the 300ms debounce (requests are ≥300ms apart) and by `list_inventory` being fast at 284 rows. The fix is the standard one (a request-id ref, or an `AbortController` through `useAdminApi`), and it belongs at the **api-layer**, not bolted onto this one page — every admin list has the identical shape |
| T4 | **The header count now silently means two different things.** `Needs a look (27)` reads as the size of the queue; with a search or a reason filter active the same number is the size of the *match*, with nothing on screen saying so | The task doc explicitly accepts this (*"the header count already reads `items.length`, so it follows the filtered result with no change"*), and the "no match" panel covers the zero case. It is only ambiguous in the non-zero filtered case — `(3)` under a search looks like a three-card queue. A qualifier next to the count ("3 of 27") needs an unfiltered total the page does not currently fetch, which is a second request for a cosmetic gain |
| T4 | **`_matches_name` searches `notes`, and no admin surface says so.** It is the reason a JP card with no readable name is findable here at all (an admin's note is often its only English text), but the placeholder reads "Search by card name…" and the queue gives no hint that a note matched | Genuinely useful behaviour that is invisible: an admin who searches a word they remember typing in a note will find the card and not know why, and one who does not know the behaviour exists will never try. Not fixed here because the honest fix is either a wider label (which then over-promises on pages where `name` really is name-only) or a match indicator on the row, and both are decisions about `_matches_name`'s contract across **every** admin list, not about Triage |
| T5 | **`/admin/sell` mounts `CardDetailModal` and nothing can ever open it.** `setDetailItem` is called in exactly one place — `onClose={() => setDetailItem(null)}` — so `detailItem` is permanently `null` and the modal returns `null` on every render (`sell/page.tsx:90, 601`). The cart rows and the search results have no `onClick`, and the page's own large-image preview (`previewItem`) is a different, read-only surface | Found while wiring the six mounting pages. Out of scope: making it reachable is a *new* interaction on the sale flow — which row opens it, whether editing an item mid-sale is even wanted while its `agreed_price` is staged elsewhere — not the live-update defect T5 was given. T5 still wired Sell's `onUpdated` correctly (it patches **both** the staged cart and the search results), so the page is right the moment a row click is added. Note the consequence for the record: **"Send to Triage" does not actually reach Sell**, so CLAUDE.md's list of six pages that mount the modal overstates the reach by one |
| T5 | **A patched row can no longer match the filter that fetched it, and stays on screen until the next fetch.** Inventory filtered to `status=available` keeps showing a card the modal just moved to `on_hold`; Show Prep's mispriced list keeps a row whose new price is no longer mispriced; Vault's summary totals (`total_cost_basis`, `total_market_value`) do not move when a cost basis is edited | Inherent to patch-instead-of-refetch, and the trade the task doc makes deliberately — a refetch is what re-mounts the table and throws the admin back to the top, which is the *reported* bug. Self-corrects on the next fetch, and the two pages where staleness would actually mislead (Triage, Prep Queue) already carry an explicit drop rule. Doing better means a per-page "does this row still match?" predicate, which is a real feature (every page would need its filter expressed twice) and should be decided once, for all of them, rather than invented per page |
| T5 | **`TriageRowAction` still refetches the whole list**, so the Prep Queue's one-click flag has exactly the defect T5 removed from the modal beside it (`outgoing/page.tsx`, `onChanged={fetchItems}`) | Different component and a different contract — `onChanged` takes no argument and the action already has an undo of its own, so changing it is a second signature change across its own call sites. Cheap to do (it PUTs the same endpoint and gets the same item back) and worth folding into whichever task next touches that component; not smuggled into T5, whose scope is the six pages that mount `CardDetailModal` |
| T6 | **`/admin/card/[id]` still has the un-fixed layout**, and it is now measurably behind: it carries `grid-cols-1 md:grid-cols-2` field grids and `w-32 flex-shrink-0` labels, i.e. the same viewport-keyed squeeze plus a label a third wider than the modal's | The planning row above already filed the duplication; this records that T6 **widened the gap** rather than closing it, because the task doc bans touching that page. The right fix is still to share a component, and the shared thing now exists in usable form — the four class decisions (auto-fit template with the inner `min()`, `col-span-full`, `flex-wrap` cell, `min-w-[min(8rem,100%)]` editor floor) are a self-contained contract that could be lifted into a `FieldGrid`/`FieldRow` pair without moving any behaviour |
| T6 | **The label is still a fixed `w-24` and can wrap to three lines in a narrow cell.** `Name Override (wins everywhere)` and `Market at Purchase` are the worst — the row grows taller instead of the label truncating, so a stacked cell is ~2 lines of label above 1 line of input | Deliberate: truncating a label makes the field unidentifiable, which is worse than a tall row, and the row is what the operator reads to know *which* field they are editing. Filed because the honest fix is shorter labels — a copy decision, not a layout one — and because the next person who sees a three-line label will otherwise reach for `truncate` |
| T6 | **jsdom cannot verify any of this, and the tests say so in their own header.** All six new assertions are class-string contracts; the actual claim — that the Finish input is typeable at 175% zoom in Chrome — is unverifiable in the suite | Structural, not a gap to close here: vitest+jsdom does no layout, so `getBoundingClientRect()` is all zeros and a real assertion is impossible. A browser-driven check (Playwright at three zoom levels against the six mounting pages) is the only thing that would actually pin it, and standing that up is its own piece of work. The **build-output grep** in this task's verification is the closest cheap proxy and is worth repeating for any future arbitrary-value Tailwind class |
| T6 | **The remaining `sm:`-keyed grids in the admin have the identical viewport-vs-container defect.** `grid-cols-1 sm:grid-cols-2` and its `md:` sibling appear across the admin pages' filter panels and detail cards; T6 converted only the two grids inside `CardDetailModal` | Same class as the reported bug and the same one-class fix, but T6's scope is the modal the owner reported. A sweep needs each site checked individually — a filter panel that collapses to one column at a narrow *container* may be right or may waste a wide screen, and the 17rem floor is tuned to this modal's label+input pair, not to arbitrary content |
| T5 | **The Triage page now predicts a row's reasons in three places and the modal handler is the fourth.** `clearReview`, `onRepointed`, `onNamed` and now `onUpdated` each do patch → `reasonsFor` → drop-if-empty, with slightly different spellings | Not fixed because collapsing them into one `applyRepair(itemId, patch)` helper touches three working code paths with their own error handling, for a readability gain — and T6 is queued on this same area. The duplication is bounded and each copy is three lines. Recorded so the next reader knows the fourth copy was noticed, not missed |
| T7 | **The bulk sticker apply still refetches the whole queue**, so it has exactly the defect T7 removed from the inline editor one function above it (`handleBulkStickerApply`, `outgoing/page.tsx`) | Deliberately left: the bulk path loops PUTs with a per-item `catch` and counts only successes, so dropping rows locally means tracking *which* ids actually landed — real logic, not the two-line patch the inline path needed. It is also the one action that already clears the selection and announces a count, so a full reload is less jarring there. Worth folding into whichever task next touches this page's bulk bar |
| T7 | **The empty message does not mention the active filter.** With the glass case selected and every glass card priced, the table reads *"No items awaiting sticker prices"* — a claim about the whole queue that the filtered request never made | Same false-congratulation class as T4's reason-filter row above, and it should be fixed the same way and at the same time. Softer here than on Triage, because the summary card directly above it now reads **"In queue (Glass): 0"**, so the scope is on screen — which is why T7 fixed the counts (its named requirement) and filed this. The generalised fix T4's row asks for — one "nothing matches the filters you have set" panel — would cover both pages |
| T7 | **Two in-flight queue fetches can resolve out of order here too**, and T7 makes it reachable: the location `<select>` and three sortable headers all refetch with no debounce, no cancellation and no sequence guard, so clicking a header twice quickly can leave the first response's rows under the second response's indicator | Identical to the T4 row above, on a different page, and filed the same way for the same reason: the fix belongs at the **api layer** (a request-id ref or an `AbortController` through `useAdminApi`), not bolted onto one page — every admin list has this shape. Narrower in practice than Triage's, because a `<select>` and a header cannot be operated as fast as a keyboard, and `list_inventory` is fast at 284 rows |
| T7 | **The manual check in the task doc was NOT performed** — filtering to the real glass case, pricing two cards without leaving the filter, and confirming the count drops by two and the remaining rows are still glass-case items | It needs the running app against live inventory. The suite pins each half (the param is sent, the priced row leaves, no refetch fires, the count is location-scoped) but jsdom cannot show that the list does not visually jump. Cheap for the owner to do in one sitting on `/admin/outgoing` |
| T16 | **`localToday()` lives in `frontend/lib/valuation.ts` and is T8's job to absorb.** T16 writes exactly one date — the `Hand-valued <date>` stamp in `value_note` — and would not use `toISOString().split('T')[0]`, which is the UTC date that turns a 6:30pm Pacific edit into tomorrow. So it built a five-line local-date helper in the wrong module | **T8 should move it, not re-derive it**, and should note that `localToday` deliberately takes an injectable `now` so a test can pin a negative-offset instant without touching the clock. Left where it is because T8 is the task that decides where the shared date helper lives and what its fallback zone is (`America/Los_Angeles`, IANA, never a fixed `-08:00`), and pre-empting that from a valuation task is how two date helpers come to exist |
| T16 | **The hand-valuation tool exists on Triage ONLY.** An unlinked card can be valued from `/admin/triage`, but the same card opened from Inventory, Prep Queue, Vault or Show Prep offers no guided path — just the modal's raw `current_market_value` row, with no condition helper and no generated `value_note` | The doc places the tool on Triage deliberately (it is the `missing_card_id` queue, and after T7 the Prep Queue can be filtered to the shelf being priced), and `CardDetailModal` *does* now carry the hand-valued marker on all six mounting pages, so the state is visible everywhere even though the tool is not. Widening it means deciding whether the dialog belongs **inside** the modal — which is the same "share a component with `/admin/card/[id]`" question T6 filed — rather than being bolted onto five more pages |
| T16 | **A hand-typed value survives the nightly job but NOT a re-point.** Value a card by hand, then link it to a catalog card from the same page, and the next `refresh_inventory_market_values` overwrites `current_market_value` **and** `value_note` with the provider figure — silently, and correctly by the current rules, but the `Hand-valued <date>` provenance is destroyed with no warning | Genuinely the right default (once a card is linked, the sync knows better than a comp typed in a hurry), and the re-point dialog already says *"The stored market price still belongs to the old card until the next sync runs."* Not fixed because the honest fix is a sentence in that dialog for the hand-valued case specifically, and the re-point flow is **T3's** surface — the same scope-drift line T4 drew. Worth one line of copy whenever that dialog is next opened |
| T16 | **`items_market_priced` counts a DANGLING `card_id` as market-derived.** The split keys on whether a `card_id` is present, not on whether it resolves — so an item pointing at a phantom catalog row is reported as market-priced even though nothing will ever price it. It is also excluded from `items_hand_valued`, so it hides in the one bucket where it looks healthy | Pre-existing shape: `items_with_card_id` has always counted the same way, and the catalog check `PUT /admin/inventory/{id}` runs on an incoming `card_id` (`admin/inventory.py`) stops *new* ones being created, so the population is legacy rows only. Resolving each id means a catalog lookup per item inside a coverage endpoint that already scans the catalog once — affordable, but it changes what that endpoint costs, and the honest place to surface a dangling link is **Triage**, which currently does not detect one either (`missing_card_id` is `card_id is None`, not `card_id does not resolve`) |
| T16 | **The Slabs page does not render the `hand_set` source, only the backend reports it.** `GET /admin/slabs` now returns `price_source: "hand_set"` and a hand-valued slab correctly leaves `?priced=false`, but `SlabList` renders the source string as-is, so an operator sees the raw token rather than a label | Small and cosmetic — the worklist behaviour, which is what the task doc names, is correct and tested. Filed rather than done because **T12** is rewriting this page (PSA out, price-at-intake in) and will be in `SlabList` anyway; adding a label now means T12 merges around it. The vocabulary is deliberately `hand_set` and not `manual`, which already means "a human typed a graded price into the price ROW" — a different fact |
| T16 | **Creating local catalog rows is still the real answer and T16 is explicitly not it** — carried forward from the planning row above, now with a decision behind it | Recorded a second time under execution because the hand-valuation tool makes the gap *comfortable*: an admin can now work an unlinked card end to end, which removes the pressure that would otherwise have forced the catalog question. That is the right trade today and the wrong one at a hundred such cards. If it is ever built it **supersedes** T16 rather than extending it |
| T8 | **`getDefaultDateRange`'s three-month `start` is computed with `Date.setMonth`**, which normalises an overflow rather than rejecting it — on 31 May it lands on 2 or 3 March, not 28 February | Correct enough for a *default* range the admin can change, and unchanged by T8 (only the ISO extraction moved from UTC to local). Filed because if any real reporting boundary is ever derived this way, the drift is silent and lands on month ends only |
| T8 | **The Sanity/public side of the site was not audited for the same defect.** T8's greps were scoped to `frontend/app/(admin)` and `frontend/components/admin`, which is where all four reported symptoms live | `/shows` renders event dates from Sanity and could carry the identical `new Date("YYYY-MM-DD")` construct. A five-minute grep the next time anyone is in that code; not swept here because the public pages have no test coverage for dates and adding it is its own piece of work |
| T8 | **`formatTimestamp` is exported and tested but has NO caller yet** | Written because the task doc specifies it and because the "local first, Pacific fallback" rule needs a single home before `voided_at` arrives — **T11 renders exactly that field** and should use this rather than growing a fourth date helper. If T11 lands without it, delete the function rather than leaving dead code |
| T9 | **`getEventLabel` on History knows `buy`/`sell` aliases that `SignedAmount` deliberately does not** (`history/page.tsx:203-221`). A hypothetical `sell` event would render the label "Sold" with no `+` | `TransactionType` is `sale`/`purchase` only, so nothing emits those aliases today — the label helper's extra cases are legacy defensiveness. Teaching `SignedAmount` about them would mean inventing a direction for a type the enum does not contain, which is the exact thing the task doc bans. If the aliases are real, the fix is to delete them from the label helper, not add them here |
| T9 | **`trade_in` and `trade_out` timeline events render unsigned**, though they carry an amount and are visibly directional to a reader | Honest, not lazy: the amount on a trade leg is the agreed value of the card moving, and whether that is money in or out depends on which side of the trade you are reading. Guessing is worse than showing none, per the task doc's own rule. If a direction is wanted it should come from the backend as an explicit field, not be inferred from an event name |
| T9 | **`PriceDisplay` still renders a raw negative as `$-160.00`** wherever a signed figure reaches it without `SignedAmount` — the sign lands between the `$` and the digits | T9 converted every such site it found on Analytics (Net Sales ×2, the per-show Net, the period Net Profit). Others may exist: History's Chain Profit and step profit hand-roll their own `parseFloat` + `toFixed` with a colour class, which is the same colour-only problem in a third spelling. Not swept because those are profit figures with their own formatting and a `+`/`−` there is a copy decision, not a defect |
| T10 | **The expanded legs show `item_id`, not the card name.** The task doc's mock shows names; `GET /admin/transactions` returns `Transaction.model_dump`, which carries `item_id` and nothing else | The doc explicitly permits *"either accept ids in the legs for this task or batch a name lookup"*, and the flat table it replaced showed no name either, so nothing regressed. Doing it properly means a batched id→name lookup in the shape of `useCardImages` (never one request per leg) — and the same batch could carry the art, which would make the archive obey the card-picker rule. Its own task |
| T10 | **A group whose legs mix `sale` and `purchase` — i.e. a trade — renders type `trade` and a NET total.** For a trade that is right, but the label is derived from *"every leg shares one `trade_id`"*, not from anything authoritative | A batch that mixed directions without a trade behind it would render as `mixed`, which is at least honest. There is no such writer today: the three confirm paths each emit one direction, except trades. Recorded because a future "sale with a refund leg" would land here and want a real answer |
| T10 | **`batch_id` is written but nothing READS it server-side.** `summarize_transactions`, the show snapshot generator and the dashboard all still work per row | Correct for T10 — the grouping is a display change and the aggregates are unaffected by it. It matters for **T11**: "void the whole transaction" needs a countability predicate that can select a batch, and `batch_id` is now the key to do it with. T11 should not invent a second one |
| T10 | **No `GET /admin/transactions?batch_id=` filter exists**, so the only way to see a batch is to fetch a date range and group it client-side | Deliberate — the endpoint's raw-archive contract is the thing T10 refuses to bend, and one day's rows are a small list. If T11 needs to load one batch by id (to confirm a void), that is the moment to add the parameter, and it should be a filter on the existing endpoint rather than a nested response shape |
| T11 | **Voiding a PURCHASE is refused with a 400, by decision — so a mistaken BUY still has no correction path.** The honest remedy today is `DELETE /admin/inventory/{id}`, which soft-deletes to `LOST` (a lie about what happened) or hard-deletes (leaving the PURCHASE row counting toward the day's spend forever) | The owner question was open and the sleeping-owner call was the honest small version: a void that returns an item to stock is well defined, one that *removes* an item which may since have been sold, traded or re-priced is not. Doing it properly needs a rule for each of those cases and probably a new terminal status ("entered in error"), which is a design pass. Recorded because a buy is at least as easy to mistype as a sale |
| T11 | **A TRADE cannot be voided at all.** Its legs share one `batch_id` but one of them is a PURCHASE, so `_guard_voidable` refuses the batch, and the single-leg route refuses the purchase leg — which is right, because voiding half a trade would leave the incoming card in stock with nothing paid for it | Follows directly from the purchase decision above and should be reconsidered with it. The trade case is actually the *cleaner* of the two — both sides of a trade are known and symmetric — so if purchase voiding is ever built, trades are the place to start |
| T11 | **`repo.get_transaction` has no index: it walks up to 37 month partitions newest-first looking for a `txn_id`.** Same for `list_transactions_by_batch` | Bounded and rare (a human presses Void), and the newest-first walk makes the ordinary case — correcting a mistake made minutes ago — a single Query. The clean fix is the `GET /admin/transactions?batch_id=` filter T10's own follow-up row proposes, plus passing the row's `date` from the client so the key is fully determined. Both are cheap; neither was needed to make the feature correct |
| T11 | **A batch of more than 50 legs is refused with a 422 telling the operator to void them one at a time.** DynamoDB caps a transaction at 100 actions and a reversal spends two per leg | Chunking would reintroduce the partial write the atomic write exists to prevent, so refusing is the safe answer — but it is a dead end for anyone who really does sell 51 cards in one session. The real fix is a resumable batch void with its own state, which is a feature, not a limit adjustment. Implausible today: the largest live session is a handful of cards |
| T11 | **The void writes its timeline event and marks snapshots stale AFTER the atomic money write, un-guarded.** A crash in between leaves a correctly voided transaction with no timeline event and a snapshot still claiming to be current | Deliberate ordering: the money must be right first, and a timeline event is not money. Both are self-healing in practice — the archive row itself carries `voided_at`, and regenerating a snapshot recomputes from countable rows regardless of the flag. Fixing it means putting the event inside the transaction (it is a third and fourth write per leg, pushing the 50-leg cap down to 25) or a retry queue |
| T11 | **`ShowAnalyticsSnapshot.stale` is set by a void but by nothing else.** Editing an item's cost basis, re-pointing a card or importing rows can all move the numbers behind a snapshot and none of them flags it | Recorded because the field now *implies* a guarantee it does not make: "not stale" means "no void has touched this", not "this is current". Widening it means every write path that can move an aggregate has to know about snapshots, which is an argument for making snapshots derived rather than stored — the planning row above already files that |
| T11 | **The archive's group total now excludes voided legs, but `GET /admin/transactions` still returns them unpaged inside the 500-row cap.** A day with many voided rows spends its cap on rows that contribute nothing | Correct by contract — the archive shows what was written — and irrelevant at this business's volume. Worth knowing if the cap is ever hit |
| T11 | **`admin_item_history` (`GET /inventory/{id}/history`) does not filter voided rows and says nothing about it.** It is a per-item history rather than an aggregate, so including them is right — but it is the one `list_transactions` call site with no comment stating which side of the rule it is on | One line of comment away from being unambiguous; left alone because touching it means deciding whether its consumers treat it as a ledger or an archive, and nothing in the frontend reads it today |
| T12 | **`GET /admin/slabs` still renders `price_source: "hand_set"` as a raw token** — carried forward from T16's row, which expected T12 to be in `SlabList` anyway. T12 was not: it touched the page, the entry form and the staging table, and left `SlabList` alone | The T16 row's premise was right about the task and wrong about the file. Still small and still cosmetic; still worth doing whenever `SlabList` is next opened |
| T12 | **The pricing poll is a bare `setTimeout` chain with no `AbortController`.** Navigating away mid-poll leaves up to 20 scheduled status GETs; the unmount effect clears the pending timer, but a request already in flight resolves into a `setState` on an unmounted component | Harmless in React 18 (the warning was removed) and bounded at ~30 seconds. Copied deliberately from the Market page's polling UI, which the task doc names as the precedent to reuse rather than a second pattern to invent — so the fix belongs to both pages at once, in `useAdminApi` |
| T12 | **A slab whose commit succeeded but whose pricing 409'd is never retried automatically.** The message tells the operator it is first in line tonight, which is true — `refresh_graded_prices` orders never-priced first — but nothing re-presses the button | Correct behaviour, recorded because "first in line tonight" is a promise the nightly job keeps only if it runs. It is also the honest limit of price-at-intake: the ceiling is FIFTY lookups a day shared with that job, so a retry loop would be the wrong instinct |
| T12 | **`confirm_buy_session` now returns `item_ids` and only the Slabs page reads it.** `/admin/buy` ignores it | Purely additive and free for the Buy page, but it is now a contract two callers can drift on. Worth knowing if buy ever wants the same immediate-pricing behaviour for raw singles — it cannot have it, because a raw single is priced from the catalog, not from a metered per-grade lookup |
| T12 | **The wedge-scanner manual check was NOT performed** — scanning a real cert into the plain field and confirming the digits land intact and focus advances | It is the behaviour Part 2 bets on and it cannot be verified any other way: the suite pins `onEnter` advancing and the `\r\n` strip, but only a physical scanner proves the whole burst survives. Cheap for the owner: open `/admin/slabs`, press Manual entry, scan one slab |
| T13 | **Groups default to OPEN, so the sidebar is TALLER than the flat list it replaced** — sixteen links plus three headers, where there were sixteen links | Deliberate, and it is the trade the "force the active group open" rule already implies: every destination has to be reachable on a first visit or the grouping is a regression. The nav has had `overflow-y-auto` since RFC 0008 §F2 so it scrolls rather than pushing the sign-out button off screen. An admin who wants it shorter shuts two groups once and the versioned key remembers it. Worth revisiting only if the owner reports the height |
| T13 | **The rolled-up badge is Triage's alone.** `rollUp` is computed from `group.items.some(i => i.badge)` and `triageCount`, so a second badged tab in the same group would show Triage's number under that group's header | One badge exists and nothing plans a second. The honest generalisation is a per-item count map keyed by href rather than one `triageCount` in the shell, which is a state change worth making at the moment a second badge is actually added — not speculatively |
| T13 | **At 60px the group dividers are unlabelled to the eye** — a `title` tooltip and an `aria-label` carry the name, but nothing is visible | The doc's own constraint ("not truncated text"), and the alternative — three-letter stubs — is what it rules out. It does mean a keyboard user tabbing through the collapsed sidebar hits three toggles they cannot see; they are announced correctly by a screen reader, so this is a visual gap, not an accessibility one. If it grates, the fix is an icon per group, which means inventing three icons that do not collide with the sixteen already there |
| T13 | **The manual check was NOT performed** — clicking through all sixteen tabs, collapsing the sidebar, confirming the Triage count is still visible, reloading to confirm the expansion state survived, and checking a phone-width viewport | jsdom asserts structure, not layout: it cannot show that a shut group's divider reads as a divider rather than a squashed row, or that the bottom nav's five entries fit a 375px screen. Cheap for the owner — one pass through the admin |
| T14 | **`.env.example` documents neither `CATALOG_REFRESH_CARDS_PER_NIGHT` nor `CATALOG_PRICE_STALE_DAYS`, and both are live `Settings` fields.** T17 added the first (default 5500) as the knob that paces the weekly catalog cycle; the second (default 30) governs staleness. An operator tuning the nightly budget has no way to discover either from the template | Found by T14's new `test_env_example_advertises_no_setting_that_nothing_reads` guard, which checks the *other* direction — that nothing advertised is inert. The reverse assertion (every `Settings` field is documented) was deliberately **not** added, because `admin_api_key` is a credential that arguably should not be advertised in a template, and deciding which fields belong there is a judgement call rather than a mechanical one. T14's named scope was removing `PSA_API_KEY`, not adding knobs, so adding two would have been scope drift on a docs task. Two comment blocks and two lines whenever someone next opens the file |
| T14 | **`docs/plans/rfc-0009/t8-docs-and-ops.md` still contains a literal `PSA_API_KEY=` line in its quoted `.env.example` block**, which is now the only place in the repo that shape appears | Harmless — it is a historical task doc quoting what the template used to look like, it carries no value after the `=`, and RFC 0009's corrected leak sweep excludes `docs/plans/` for exactly this reason. Recorded only so the next person running a *naive* `grep -rn "PSA_API_KEY="` knows the one hit is a quotation, not a regression |
| T-FINAL | **The frontend suite's wall time is dominated by environment setup, not by tests** — this run: 61.6s total against `tests 111.31s` / `environment 260.10s` / `setup 128.16s` across parallel workers | Not a regression and not on any critical path; recorded because the numbers look alarming next to the ~33s serial baseline and the explanation (concurrent execution with the backend suite, plus per-worker jsdom construction) is not obvious from the summary line. CLAUDE.md already records the `// @vitest-environment node` mitigation for pure-logic files; the remaining cost is the ~67 files that genuinely need a DOM |
| T-FINAL | **`npm run lint --workspace=frontend` warns that `next lint` is deprecated and removed in Next.js 16**, recommending a codemod to the ESLint CLI | Cosmetic today and the command still exits 0, but it is a scheduled break rather than a style nit: the documented lint command in CLAUDE.md's Test Commands table stops working at the Next 16 upgrade. Worth doing as part of that upgrade, not before — migrating the linter independently means two config surfaces to keep green for no benefit |
| T-FINAL | **RFC 0010's task docs were wrong in 17 of 18 cases** (T6 alone was clean): wrong test paths, `-k` expressions that collect nothing, the broken `npx vitest` form, an unachievable grep gate, a stale "expect 6–7 failures" premise, and a boot command that would have printed a pass without testing anything | Every instance was corrected **in the task doc itself** as it was executed, from T2 onward, so this is closed for RFC 0010. Filed as a **process** finding for whoever writes RFC 0011's plan: the docs were all written up-front, before any of them had been run, and none was revised afterwards. The cheap fix is to write each task doc's verification command by **running it once** at planning time against the then-current tree — a wrong path fails loudly, but a wrong premise or a mis-scoped `-k` certifies work that was never tested |
