# RFC 0008 — Follow-up ledger

Things found while executing RFC 0008 that were **deliberately not fixed in the
task that found them**, collected here so the owner can triage them in one pass
once T1–T11 and T-FINAL are done.

This is not a bug tracker and not a backlog. It is the "we noticed this, it was
out of scope, someone should decide" list.

## For agents adding to this file

Append to the table for your task, creating the section if it does not exist.
One row per finding. Keep it to what a reader needs to decide:

- **Finding** — what is wrong, in one line, with a `file:line` link.
- **Why deferred** — the actual reason (out of scope / needs an owner decision /
  needs infra access / risky to change blind). "No time" is not a reason.
- **Impact if ignored** — be honest, including "probably none".

Rules:
- **Do not fix items in this file as a side errand.** They are here precisely
  because they were judged out of scope. If one blocks your task, say so in your
  report and ask.
- **Do not delete rows.** Mark a resolved row `~~struck through~~` with the task
  that handled it, so the owner can see it was dealt with rather than lost.
- Pre-existing problems your change merely *revealed* belong here. Problems your
  change *caused* do not — fix those.
- If a row turns out to be wrong on closer inspection, say so in the row rather
  than removing it.

---

## T9 — Catalog search (done 2026-08-05)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| ~~**ECS container memory is unverified against the new catalog cache.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — measured: the `merlins-merlins-backend` task definition (rev 13) is **cpu 1024 / memory 2048 MB**. The 93 MB cache is ~4.5% of that. No OOM risk.  The in-process cache measures **92.6 MB** resident for 31,603 cards (the plan's "11.7 MB" was the DynamoDB wire size; parsed pydantic models cost ~8x that). `deploy/backend-container.json` declares no memory limit — it is set at ECS service creation, which is outside the repo. | Needs AWS console access to read the task definition's memory setting. Cannot be checked or fixed from the repo. | If the task is sized at 512 MB, the API may OOM once the cache warms. Python + FastAPI + boto3 baseline plus 93 MB is tight. Lowering `TTL_SECONDS` does **not** help — steady-state footprint is the same. |
| **Nested `<button>` on the Market page.** The watchlist Star button sits inside the card button — [market/page.tsx:490](../../../frontend/app/(admin)/admin/market/page.tsx#L490). Invalid HTML; React logs `validateDOMNesting` whenever results render. | Pre-existing, not caused by T9 — T9's new test is simply the first to render catalog results and surface it. Fixing it changes click semantics on a surface the owner has to sign off on. | Works today under client-side React (no HTML-parser reparenting). A future move to SSR/RSC for this page would reparent the inner button and could break "add to watchlist". |
| ~~**`ruff check backend/src` reports 29 pre-existing errors**~~ **RESOLVED (T-FINAL, 2026-08-06)** — all 28 fixed; `ruff check backend/src` now reports **All checks passed**. 9 auto-fixed, the `l`→`leg` renames and line wraps by hand. , 10 auto-fixable (e.g. an unused `ShowAnalyticsSnapshot` import in `services/dynamodb.py`). None are in files T9 touched. | Repo-wide lint debt, unrelated to any single task. Fixing it inside a task doc's diff would bury the actual change. | Lint stays noisy, so a genuinely new error is easy to miss in the output. Worth one dedicated cleanup pass. |
| ~~**`python -m pytest` from CLAUDE.md's test table does not work.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — CLAUDE.md's Test Commands table now says `./.venv/Scripts/python.exe`, with the worktree check.  `python` on PATH resolves to a hermes-agent venv with no pytest. The working invocation is `./.venv/Scripts/python.exe -m pytest ...`. | Documentation fix; belongs to the `sync-docs` pass at the end rather than a feature task. | Every fresh agent loses a few minutes rediscovering it. See also the `worktree-editable-install-shadowing` memory. |

## T10 — English display names (done 2026-08-05)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| ~~**Chat mode still shows the Japanese name.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — MCP's `toCard` now honours `display_name_override` ahead of the catalog name, with 4 tests.  `/inventory` has two modes; T10 fixed only one. The MCP server reads the DynamoDB row directly and resolves a name as catalog-name → `display_name` → ULID, with no knowledge of the override — [dynamodb-repository.ts:171-179](../../../mcp-server/src/dynamodb-repository.ts#L171-L179). So filter mode renders "Chespin" and chat mode answers "ハリマロン" for the same card. | The task doc scopes T10 to **backend + frontend**; MCP is a third layer with its own repo, its own tests, and its own review surface. Widening the change there unasked would also push admin-authored free text into the Bedrock prompt, which is a decision the owner should make knowingly rather than inherit from a display fix. | The two halves of the same page disagree about what a card is called. Low blast radius (~9 JP items, of which only those given an override), but it is exactly the kind of inconsistency that reads as a bug. Fixing it is a ~3-line change plus a test in `mcp-server`. |
| ~~**The admin surfaces will not show an override.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — owner decided the override wins everywhere. Shared helpers added: `frontend/lib/admin-item-name.ts` (17 call sites across 7 pages + the column registry) and `backend/services/card_text.admin_item_name` (show_prep, market, vault, sales, trades, inventory).  Seven admin pages render the name as `display_name \|\| product_name` and none of them consult `display_name_override` — [inventory/page.tsx:226](../../../frontend/app/(admin)/admin/inventory/page.tsx#L226), [sell/page.tsx:316](../../../frontend/app/(admin)/admin/sell/page.tsx#L316), [trade/page.tsx:674](../../../frontend/app/(admin)/admin/trade/page.tsx#L674), [history/page.tsx:331](../../../frontend/app/(admin)/admin/history/page.tsx#L331), [cosigners/page.tsx:358](../../../frontend/app/(admin)/admin/cosigners/page.tsx#L358), [outgoing/page.tsx:215](../../../frontend/app/(admin)/admin/outgoing/page.tsx#L215), [CardDetailModal.tsx:118](../../../frontend/components/admin/shared/CardDetailModal.tsx#L118). T10's scope is the **customer** display path (`itemTitle`); these are a separate resolution chain that predates it. | Needs an owner decision, not a code decision. There is a real argument for the admin tables keeping the raw `display_name`: staff match against the physical sheet/binder, and silently renaming rows to the customer-facing string could make an item harder to find, not easier. | An admin sets "Chespin" on `ja:M4-084`, then still sees "Chespin #84" everywhere in the admin panel and reasonably concludes the edit did not save. Most likely to bite immediately after T11 ships the editing UI. |
| ~~**`CardDetailModal`'s editable field list offers `display_name`, not the override**~~ **RESOLVED (T-FINAL, 2026-08-06)** — the modal now shows BOTH rows: "Display Name (imported)" and "Name Override (wins everywhere)".  — [CardDetailModal.tsx:23](../../../frontend/components/admin/shared/CardDetailModal.tsx#L23). Editing it there rewrites the import-materialized fallback, which the sync/import path owns. | T11 owns the editing UI and should decide whether this modal gains an override row, loses the `display_name` row, or is left alone. Changing it inside T10 would ship an editing surface the task explicitly deferred. | An admin edits `display_name` expecting it to change the customer-facing name; for a catalog-matched item it changes nothing visible, because `card.name` outranks it. Silent no-op. |
| **`admin_update_item` has no field allowlist at all** — [admin/inventory.py:282-291](../../../backend/src/merlins_collection/routers/admin/inventory.py#L282-L291) merges the whole request body into the validated model, so every writable model field is reachable via `PUT`, including `lineage_id`, `predecessor_item_id`, and `consignment`. | Pre-existing since the endpoint was written; T10 only *relies* on it (which is why T10 pins the override's writability as an endpoint contract test rather than trusting the merge). Adding an allowlist now would need every existing admin page audited against it. | A bug or typo in any admin page can rewrite trade lineage or consignment terms through the generic edit endpoint, leaving only a generic `edit` timeline event as the trace. |
| **The task doc's backend section is wrong in two places** — it says `PATCH /admin/inventory/{id}` (the route is `PUT`) and instructs "add it to `admin_update_item`'s allowed fields" (no such list exists; see the row above). | Doc drift in `docs/plans/rfc-0008/t10-jp-english-names.md`, not in code. Belongs to the `sync-docs` pass. | A later agent adds an allowlist that does not need to exist, or hunts for a `PATCH` route that was never there. |
| **The 422 handler echoes the rejected input back to the caller** — [admin/inventory.py:290-291](../../../backend/src/merlins_collection/routers/admin/inventory.py#L290-L291) returns `detail=str(exc)`, and a pydantic `ValidationError` string embeds the submitted value. | Pre-existing for every field on the endpoint; T10's over-length test merely exercises the path. Admin-only endpoint, and the caller already knows what it just sent. | Effectively none today. It would matter if this error body ever reached a non-admin surface or a shared log sink. |

## T1 — Price filter reads the displayed price (done 2026-08-05)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| ~~**The customer-facing price ignores the condition multiplier, and now the price FILTER does too.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — owner decided customer prices MUST include the multiplier. Applied once at enrichment (`_condition_adjust`) so tile/sort/filter all inherit it, plus `/inventory/summary` and MCP. Measured live: total moved $6,143 -> $5,005 (-18.5%) over 73 of 228 items.  `apply_condition_adjustment` scales the catalog's NM figure by tier — LP ×0.82, MP ×0.58, HP ×0.33, **DMG ×0.15** ([condition_pricing.py:28-33](../../../backend/src/merlins_collection/services/condition_pricing.py#L28-L33)) — and `refresh_inventory_market_values` bakes the result into `current_market_value`. But `_display_price` reads the raw, unadjusted `card.market_price`, so a DMG card is shown to a customer at **~6.7× what the business values it at**, and MCP/chat mode (which reads `current_market_value` directly) quotes a different number for the same card. | Pre-existing and NOT caused by T1 — the tile ([CardTile.tsx:19](../../../frontend/components/inventory/CardTile.tsx#L19)) and the price sort already read the unadjusted figure. T1 only made the filter agree with them, which is what §A asked for; T1's brief is "make the three agree", not "decide which figure is right". Choosing the adjusted figure is an owner pricing decision and changes what every customer sees. | A buyer filters "under $100", sees a DMG Charizard priced at the NM figure, and the number is wrong in the business's favour — the worst direction to be wrong in. This is the same class of bug as §A one layer up, and fixing it is a one-line change in `_display_price` + `CardTile.tsx` **only if** the owner wants adjusted prices shown. |
| **Every graded slab now lands in `hidden_no_price` under a price bound.** `_display_price` deliberately skips the catalog for non-`raw` kinds (a slab's catalog price is ungraded) and falls back to `listed_price`, which is NULL on every item by owner decision — so a slab resolves to no price at all. Its `current_market_value` (written from `get_graded_market_value`, [catalog_sync.py:127-132](../../../backend/src/merlins_collection/services/catalog_sync.py#L127-L132)) is no longer consulted. | t1-price-filter.md §"Leave alone" explicitly forbids changing the graded skip. The behaviour is also *consistent*: a graded tile already renders **"Price N/A"** today, so the bound is honestly reporting what the customer can see. Fixing it means deciding whether slabs should display `current_market_value` — a display-layer decision, not a filter one. | Before T1 slabs passed price bounds on their denormalized value; now they are excluded and counted. The `hidden_no_price` affordance surfaces them rather than dropping them silently, but the count will visibly jump and graded stock becomes unreachable via the price filter. Pinned by `test_price_bound_counts_items_with_no_displayable_price` so it cannot regress unnoticed. |
| **Catalog enrichment now runs over the pre-bound cohort instead of the survivors** — [inventory.py:266-273](../../../backend/src/merlins_collection/routers/inventory.py#L266-L273). `Enriched*` models are constructed for items the price bound then discards. | Unavoidable: the bound cannot read `item.card` before `_enrich` attaches it. Not worth optimizing blind. | Almost certainly none. The cohort is already location-gated to glass/toploader, `_load_catalog` ran over that same set whenever `name`/`rarity` was set, and `/summary` + `/facets` batch-get the whole cohort unconditionally. Revisit only if the display case grows by an order of magnitude. |

## T3 — MCP chat total must match the dashboard (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **One resolution rung still has no backend equivalent: the manual `GRADEDPRICE` row.** T3 aligned the order to `/inventory/summary`'s `live → current_market_value → listed_price`, but MCP keeps an extra rung between the last two — a direct read of the slab's `GRADEDPRICE#<company>#<grade>` row ([dynamodb-repository.ts:238-244](../../../mcp-server/src/dynamodb-repository.ts#L238-L244)). The backend never reads that row at request time; `catalog_sync.get_graded_market_value` bakes it into `current_market_value` at sync time instead. So a slab whose sync has not run yet is priced by chat and **skipped** by the dashboard. | Pre-existing and deliberate — it predates T3 and has two tests of its own ("uses the manual graded price row…", "dedupes graded-price keys…"). Removing it is an owner call, not a code one: it makes chat *less* accurate in exchange for exact agreement with the dashboard. The reverse fix — teaching `/inventory/summary` to read `GRADEDPRICE` — is a backend change T3 is explicitly forbidden from making. | Narrow. Needs a slab with a `GRADEDPRICE` row, a null `current_market_value`, and the denormalizer behind — i.e. a newly graded item between sync runs. When it happens, chat quotes a real number for a slab the dashboard total omits, which is the exact class of disagreement T3 exists to remove. |
| **`uniqueSets` and the backend's `sets_tracked` count different things.** MCP counts distinct `card.set`, which falls back to the card_id prefix or the literal `"Unknown"` when there is no catalog row ([dynamodb-repository.ts:200](../../../mcp-server/src/dynamodb-repository.ts#L200)); the backend counts distinct `set_id` among items that actually **resolve** in the catalog, so a null-`card_id` item contributes nothing ([inventory.py:381](../../../backend/src/merlins_collection/routers/inventory.py#L381)). With ~9 JP items carrying `card_id=None` by design, MCP reports at least one extra "set" named `Unknown`. | T3's brief is the price walk and the totals that depend on it. This is a second, independent divergence on a different field, and unlike the price one it was not named in RFC §D — fixing it unasked would widen a bugfix into a semantics change on a number the owner has not been asked about. | Chat says "N sets" where the dashboard says "N-1", and the phantom set is called `Unknown`. Cosmetic next to the price bug, but it is the same *kind* of inconsistency and a customer comparing the two surfaces sees it. |
| **`asNumber()` still turns a non-finite stored value into a real `$0`** — [dynamodb-repository.ts:71-74](../../../mcp-server/src/dynamodb-repository.ts#L71-L74). T3 hardened the *catalog* walk against this (`bandMarket` skips a non-numeric band rather than coercing it), but `current_market_value` and `listed_price` still go through bare `asNumber`, so a corrupt row reports `$0` instead of `null`. | The catalog band was hardened because T3's inversion put it **first** and a coerced `$0` would have outranked a good stored value — a regression T3 itself would have caused. The stored-value rungs are unchanged by T3 and no worse than before; tightening them is a data-hygiene pass over rows nothing is known to have corrupted. | A malformed row is silently valued at $0 rather than surfacing as unpriced. No known instance today — the catalog sync writes pydantic-validated numbers — but the admin market page can write these fields by hand. |

## T4 — Sidebar stays put · Vendor mode deleted (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **The fix was never checked in a real browser.** `h-screen overflow-hidden` is verified by unit test and by reasoning about the flex/overflow interaction, but the task doc's two visual checks — sidebar holding while an inventory table scrolls, and the 375px mobile-nav clearance — were **not** performed. Running the admin panel needs `npm run dev` plus a Cognito session in the admin group. | Needs admin credentials and a running stack, neither of which is available from the repo. Not a judgement call — simply undone. | The class change is the RFC's prescribed fix and the unit tests pin it, but nested scroll containers (Vault's and Inventory's sticky table headers) and the fixed mobile bottom nav are exactly the things a unit test cannot see. Someone should load both widths once before this ships. |
| **`h-screen` is `100vh`, which does not account for mobile browser chrome.** Tailwind here is `^3.4.0`, so `h-dvh` (dynamic viewport height) is available and would keep the bottom of `<main>` clear of a collapsing iOS/Android URL bar. | [t4-sidebar-and-vendor-mode.md](t4-sidebar-and-vendor-mode.md) specifies `h-screen` explicitly, and the RED test asserts it. Swapping the viewport unit changes visible layout behaviour on every admin page — an owner decision, not a silent substitution inside a bugfix. | On mobile Safari the last ~60-100px of `<main>` can sit under the browser chrome, on top of the already-`fixed` bottom nav that `pb-20` exists to clear. One-word change (`h-screen` → `h-dvh`) if the mobile check above turns up trouble. |
| **The backend still writes `"mode": "customer"` on every trade session** — [admin/trades.py:176](../../../backend/src/merlins_collection/routers/admin/trades.py#L176), merged again on update at line 271. The frontend never sent it (the create call posts `{}`) and nothing reads it back for any calculation. | The task doc explicitly says to leave it: it costs nothing, keeps historical rows shape-consistent, and dropping a written field is a separate decision from removing dead UI. | None functionally. It is a dead field that will read as meaningful to the next person who greps for `mode` in the trades router and finds it next to the live `basis_mode`. |
| **`"vendor mode"` still greps in `frontend/`** — but only inside [trade/\_\_tests\_\_/page.test.tsx](<../../../frontend/app/(admin)/admin/trade/__tests__/page.test.tsx>), where the describe block and its assertions are named after the removed feature. | Intentional, not a leftover: the regression guard has to name the thing it guards against. The task's "Done when" grep was written against production code. | None. Flagged only so a future reader running that exact grep does not think the deletion was incomplete. |

## T5 — Item detail: full field coverage (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **T11's assumed insertion point is narrower than its plan believes.** `CardDetailModal`'s docstring claimed it "opens when clicking a card row in any admin page"; it is actually mounted by **five** pages — [inventory](<../../../frontend/app/(admin)/admin/inventory/page.tsx#L530>), [outgoing](<../../../frontend/app/(admin)/admin/outgoing/page.tsx#L460>), [sell](<../../../frontend/app/(admin)/admin/sell/page.tsx#L583>), [show-prep](<../../../frontend/app/(admin)/admin/show-prep/page.tsx#L567>), [vault](<../../../frontend/app/(admin)/admin/vault/page.tsx#L321>). Buy, Trade, Market, History, Cosigners, Analytics and `/admin/card/[id]` do **not** mount it. The docstring is corrected in code as part of T5. | The claim was T5's job to sanity-check, not to act on — [t5-detail-modal.md](t5-detail-modal.md) "Watch for" asks only that T11 "know that early". Where a Send-to-Triage button actually belongs is T11's design call. | A "Send to Triage" button added here is unreachable from Buy, Trade and the single-item card detail page. If triage is meant to be reachable from anywhere an admin sees a card, this modal alone does not deliver it. |
| **Consignment terms render read-only.** [t5-detail-modal.md](t5-detail-modal.md) §2 permits this ("a read-only correct display beats a broken editor") and it is what shipped. | The update endpoint merges the request body into the validated model, so a nested object is **replaced wholesale** — editing `minimum_price` alone would have to resend `consignor_id`, `split_percent` and `paid_out` or silently reset them. That is someone else's money on an item the business does not own, and it wants a deliberate sub-form with its own tests. | An admin cannot correct a consignment split from the detail modal and must go through `/admin/cosigners`. Visible and correct, just not editable. |
| **Four enum-backed fields render as free-text inputs**: `status` (`ItemStatus`), `language` (`Language`), and the two T5 added — `company` (`GradingCompany`: PSA/BGS/CGC/SGC) and `product_type` (`SealedProductType`). | Turning these into selects means hardcoding three more backend vocabularies into the frontend — which is precisely the drift problem **already logged in T2's row 2** ("the condition vocabulary is now declared in three places, nothing enforces agreement"). Compounding a known, unresolved problem inside an unrelated task is worse than leaving it visible. Solving it once (codegen, or a served vocabulary endpoint) is an owner decision. | A typo produces a **422 with the pydantic message in the error banner** — visible, not a silent no-op, which is the bar [t5-detail-modal.md](t5-detail-modal.md) sets. But an admin gets no discoverable list of what "product type" accepts and has to guess `booster_box` vs `Booster Box`. |
| ~~**Pressing Escape while editing a field closes the entire modal, discarding the edit.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — `e.stopPropagation()` added to both the text and textarea Escape handlers.  The field's own `onKeyDown` calls `cancelEdit()`, but the event keeps bubbling to the document-level Escape handler ([CardDetailModal.tsx:69-77](../../../frontend/components/admin/shared/CardDetailModal.tsx#L69-L77)) which calls `onClose()`. Both fire. | Pre-existing since the modal was written and true of every field — T5 did not cause it. The fix is one `e.stopPropagation()`, but it changes what Escape means while a modal is open, which is a UX call an owner should make rather than inherit from a field-coverage task. | Cheap before T5 (retyping a one-line value); now the same keystroke can discard four rows of typed notes, which is exactly the field issue #13 was filed about. Worth fixing early. |
| **Clearing a required field 422s with a raw pydantic string.** The generic save path sends `null` for an empty input, but `acquired_at`, `cost_basis`, `finish`, `grade`, `company` and `cert_number` are non-optional on the model, so blanking one is rejected. (Checkbox fields are exempt — T5 gives them a boolean save path precisely because `null` would 422 there too.) | Making "clear" mean "restore the default" would need a per-field notion of what the default is, and for `cost_basis` there is no safe answer. Surfacing the model's own rejection is the honest behaviour. | The admin sees a wall of pydantic text in the error banner instead of "Acquired date is required". Recoverable — nothing is written — but unpleasant. Note this also echoes T10's logged row about `detail=str(exc)` echoing input back. |

## T11 — Triage tab (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **The re-flag scenario T11 is architected against does not exist in the code yet.** Verified 2026-08-06: **no path writes `needs_review=True` onto an existing row.** `import_singles` ([spreadsheet_import.py:469](../../../backend/src/merlins_collection/services/spreadsheet_import.py#L469)) and `confirm_buy_session` ([purchases.py:219](../../../backend/src/merlins_collection/routers/admin/purchases.py#L219)) both mint a fresh ULID and CREATE an item; `run_singles_only_import` sweeps the prior generation outright, so a cleared item's `reviewed_at` is destroyed rather than respected. T11's guard therefore keys off the reason vocabulary on `PUT /admin/inventory/{id}` instead (see the module docstring of [test_triage.py](../../../backend/tests/routers/admin/test_triage.py)). | Making the import path *match* existing rows instead of re-creating them is an import-architecture change with real blast radius over 266 live rows and the generation-sweep contract — far outside a triage UI task, and not something to attempt without the owner. | The guard as built is correct but partly **dormant**: it protects against a future in-place re-matcher and against a human/UI mistake, and does nothing about the actual live re-import, which wipes `reviewed_at` along with the row. If the singles sheet is ever re-imported, every clearing decision an admin made in Triage is silently lost. |
| **`admin_update_item` is now doing policy, not just merging.** The re-flag guard and the `reviewed_at` stamp are conditional logic inside a generic "merge the body into the model" endpoint ([admin/inventory.py:267-319](../../../backend/src/merlins_collection/routers/admin/inventory.py#L267-L319)) that has **no field allowlist at all** (already logged under T10, row 4). Triage's rules are the first business rules to live there. | Splitting the review transitions onto their own endpoint (`POST /admin/triage/{id}/clear`) was rejected: the task doc says explicitly to reuse `PUT` and not build parallel routes, and the modal already writes every other field through it. | The endpoint becomes the place where field-specific policy accumulates. The next person adding a rule has no obvious home for it and will either bury it here too or start a second, inconsistent pattern. Worth revisiting together with the missing allowlist rather than separately. |
| **`GET /admin/triage/counts` is a full `list_inventory()` scan, and `AdminShell` renders on every admin page.** So every admin navigation costs one full inventory read for a number in the sidebar. | At 266 items this is genuinely cheap, and caching it invites the staleness bug the badge exists to prevent (a cleared item that still shows in the count). T9's catalog-cache experience says do not add a cache before there is a measured problem. | Nothing today. It scales with inventory count × page views, so it is the first thing to look at if the admin panel ever feels slow — not a reason to pre-optimize now. |
| **`review_reason` mixes a machine vocabulary and human free text in one column.** `MACHINE_REVIEW_REASONS` values (`low_match_confidence`, `manual_entry`, …) and an admin's "back looks trimmed" share the field, and the re-flag guard's automated/human distinction is inferred from which it looks like. | Two columns (`review_source` + `review_note`) is the cleaner model, but it doubles the migration surface over 266 live rows for a distinction only the guard consults. The task doc specifies one field. | An admin who types exactly `manual_entry` into the note box is treated as automation and cannot re-flag a reviewed item. Vanishingly unlikely, and it fails safe (the item stays cleared), but it is a real seam. Revisit if a third reason source appears. |
| **Undoing a misclick stamps `reviewed_at`, so it counts as "a human reviewed this".** Undo sends `needs_review: false`, and `_apply_review_transition` stamps the clear unconditionally — [admin/inventory.py `_apply_review_transition`](../../../backend/src/merlins_collection/routers/admin/inventory.py). So flagging by accident and immediately undoing marks the item as inspected-and-passed, which then suppresses a future *machine* flag on it. | Distinguishing "undo" from "clear" means either a second endpoint or an explicit `undo: true` flag on the body, and the whole point of the transition helper is that the endpoint stays a dumb merge with one small policy. The wrong outcome is also mild — a human genuinely did look at the card. | Latent, and only once something automated re-flags existing rows (which nothing does today — see the first row). At that point an accidentally-flagged card is permanently immune to automated re-flagging. Cheap fix if it ever matters: have the Undo path send `reviewed_at: null` explicitly. |
| ~~**Re-pointing accepts any `card_id` string; nothing checks the catalog row exists.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — `admin_update_item` rejects a `card_id` absent from the catalog with a 422. Only what the request CHANGES is checked, and `None` (unlink) stays allowed. 4 tests.  The UI only offers real search results, but `PUT /admin/inventory/{id}` has no allowlist and no validation, so a hand-crafted or stale `card_id` links an item to a phantom card. The importer guards against exactly this (`spreadsheet_import.py:430-435` validates the composite against the catalog index before storing it); this path does not. | Pre-existing gap in the endpoint (same root as T10's "no field allowlist at all" row), but T11 is what makes `card_id` editing a first-class, encouraged flow rather than a theoretical one — so it is newly *relied upon*, which is why it is logged here rather than left under T10. | An item pointing at a nonexistent card resolves no price, no image and no set — it reappears in Triage looking unlinked while actually carrying a bad id, which is more confusing than the null it replaced. One `repo.get_catalog_card()` check in `admin_update_item` would close it. |
| **The sidebar badge refetches the whole inventory on every admin navigation.** `AdminShell` mounts on every admin page and its effect is keyed on `pathname` — [AdminShell.tsx](../../../frontend/components/admin/AdminShell.tsx). | Deliberate: caching the count reintroduces exactly the staleness the badge exists to prevent (a cleared card still showing in the count). At ~266 items the read is cheap. | Cost scales with inventory × page views. First thing to look at if the admin panel ever feels sluggish; see also the `/admin/triage/counts` row above. |
| ~~**Nothing writes the machine reasons yet.**~~ **RESOLVED GOING FORWARD (T-FINAL, 2026-08-06)** — `_review_reason_for_row` (import) and `_review_reason_for_buy` (Buy) now emit reasons from `MACHINE_REVIEW_REASONS`. The 21 pre-existing flagged rows still show a bare chip — confirmed live 2026-08-06, `review_reason` is null on all of them — and cannot be backfilled.  `MACHINE_REVIEW_REASONS` is pinned by test and consumed by the guard, but `import_singles` and `confirm_buy_session` still set the bare boolean with no reason — so the 21 already-flagged live items will show in Triage with **no chip explaining why**. | Backfilling a reason onto rows written before the field existed means guessing intent from data that no longer distinguishes the cases (`spreadsheet_import.py:439-444` collapses medium/low/empty confidence and a null `card_id` into one boolean). A wrong guess is worse than a blank. | The first Triage session shows 21 cards with an unexplained "flagged" chip — exactly the "queue with no stated problem" the task doc calls "not a worklist". Teaching the two setters to pass a reason is small and could ride along; backfilling the existing 21 cannot. |

## T7 — Shows CRUD, archive instead of delete (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **Existing show rows may already be duplicated, and T7 does not backfill.** `put_show` writes `SK=SHOW#{date}#{show_id}` — [dynamodb.py:1085](../../../backend/src/merlins_collection/services/dynamodb.py#L1085). T7 makes it sweep superseded rows for the same `show_id` *on write*, which fixes every edit from now on, but it does nothing to rows that are already forked. Any show the spreadsheet import wrote across two committed generations, or under two dates, is still two rows until someone next saves it. | A one-shot dedup over live data needs AWS creds and a judgement call about which of two rows wins when they disagree (newest generation? latest date?). That is a data-migration script, not a feature task, and guessing wrong silently discards a show's `cash_at_start`/`notes`. | Probably none — `finalize_import`'s commit sweep deletes non-current generations, so a clean import history leaves one row per show. But if any duplicate does exist, the Shows page lists the show twice and archiving hides only one copy. Self-heals the moment an admin edits or archives the show. A read-only `list_shows()` duplicate check would confirm in one command. |
| **Nothing stops two admins from clobbering each other's show edits.** `PUT /admin/shows/{id}` is a read-merge-write with no precondition — [analytics.py `_save_show`](../../../backend/src/merlins_collection/routers/admin/analytics.py). The sibling admin-managed list, Locations, does guard this: `put_location_config` takes an `expected_generation` and a lost race surfaces as a 409 ([locations.py:62-67](../../../backend/src/merlins_collection/routers/admin/locations.py#L62-L67)). Shows have no equivalent. | Not worse than before — until T7 nothing outside the one-time import could write a show at all, so there was no race to lose. Adding optimistic concurrency means a version attribute on the show row and a conditional put, i.e. a storage change, which is more than a CRUD task should smuggle in. | Two admins editing the same show at a card show both get a 200 and the second silently wins. Realistically a two-person business editing a dozen shows a year; the cost of being wrong is a re-typed venue, not lost money. Revisit if show editing ever becomes concurrent in practice. |
| **`PUT` can set `archived` directly, bypassing the dedicated endpoints.** `_save_show` merges the whole request body, so `{"archived": true}` on the update route does exactly what `POST /shows/{id}/archive` does. | Deliberate: the merge is generic by design (same shape as `admin_update_item`), and adding a field allowlist here would be the first one in the admin package — see T10 row 4, where the *absence* of any allowlist is already logged as the open question. Solving it for one router only makes the inconsistency worse. | None today; both paths reach the same validated write. It matters only if archiving ever grows side effects (an audit event, a notification) that the archive endpoint performs and the generic `PUT` would skip. |
| **The Shows page was never opened in a browser.** All nine frontend behaviours are pinned by vitest against a mocked `useAdminApi`; nothing rendered the real page against the real API. | Needs `npm run dev` plus a Cognito session in the admin group — neither is available from the repo. Same constraint already logged under T4 row 1. | The date `<input type="date">` round-trip (browser value ↔ the ISO string the backend wants) and the `ConfirmDialog` `<dialog>` element are the two things a jsdom test models rather than exercises. Worth one manual create-edit-archive-restore pass before this ships. |
| **The sidebar is now 15 items and the mobile bottom nav still shows only the first five.** `navItems.slice(0, 5)` — [AdminShell.tsx:162](../../../frontend/components/admin/AdminShell.tsx#L162). Shows is item 9, so it is unreachable on mobile. | Pre-existing and true of ten other tabs (Market, Triage, History, Cosigners…). Deciding what mobile admins actually need is a product call, not something a CRUD task should settle by picking a different slice index. | Ten admin pages including this one have no mobile route. Whether that matters depends entirely on whether the owner uses the admin panel from a phone at a show — which is exactly the situation Shows CRUD is for. Worth asking. |

## T6 — Configurable columns, filters that follow them (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **Hiding the Name column hides the search box.** `search` follows `display_name` like every other filter — [admin-inventory-columns.tsx `INVENTORY_FILTERS`](../../../frontend/lib/admin-inventory-columns.tsx). So an admin who unticks "Name" loses the only free-text search on the page, and the hidden-column notice only appears once a search is already active. | This is the owner's Q9 rule applied literally, and [t6-column-registry.md](t6-column-registry.md) says to build exactly that. Pinning the search box would be a carve-out the owner did not ask for, and carve-outs are how "filters follow columns" stops meaning anything. | Recoverable in two clicks (re-tick Name, or hit "Show all filters"), but the first time it happens it reads as the search box vanishing for no reason. One line to pin it if the owner would rather. |
| ~~**The Condition column still shows the bare tier, never `LP+`/`LP-`.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — the registry cell now calls `formatCondition(condition, condition_modifier)`.  The registry preserves the old cell verbatim — `item.condition ?? '—'` — while the item also carries `condition_modifier`, and `formatCondition` ([constants.ts:38](../../../frontend/lib/constants.ts#L38)) exists precisely to join them. An LP+ and an LP- card are indistinguishable in the table. | Pre-existing, and T6's brief is to MOVE the columns, not restate them — changing what a cell displays inside a mechanical relocation is how a regression gets attributed to the wrong task. The registry now makes it a one-line change in one place, which it was not before. | T2 went to real trouble making the backend emit and filter `LP+`/`LP-`; the admin table is the one surface that still cannot show the distinction. Directly undercuts that work. Cheap to fix deliberately. |
| **`set_name` / `card_number` / `artist` are filter-only, with no column to follow.** The ordinary admin search response carries no catalog join — `_attach_catalog_cards` is scoped to `triage=true` ([admin/inventory.py:241](../../../backend/src/merlins_collection/routers/admin/inventory.py#L241)) — so a Set or Artist column would render an em dash on every row. They are bound to `columnKey: null`, which means they now live behind "Show all filters" instead of being on the panel by default. | Shipping three permanently-empty columns to satisfy the rule literally would be worse than admitting the data is not there. Widening the search response to join the catalog for every request is a backend change with a real cost (T9's cache experience) and is not T6's call. | Three filters moved from "always visible" to "one click away", and an admin who used them regularly has to find them. Any one left active IS called out by the hidden-column notice, so it can never filter silently. Revisit if the owner wants a Set column enough to pay for the join. |
| **Column choice is per-browser and does not survive a cache clear or reach a second device.** `localStorage`, keyed `admin-inventory-columns-v1`. Two tabs open at once will also clobber each other — last toggle wins, with no cross-tab `storage` listener. | Explicitly accepted in the task doc ("No backend schema, no cross-device sync — owner accepted; RFC alternatives"). The cross-tab case is the same class as the cross-device one. | An admin who works from a laptop and a phone configures the table twice. Nothing is lost but the preference. A `storage` event listener would fix the two-tab case in ~5 lines if it ever bites. |
| **34 columns is a lot of checkboxes with no grouping or search.** The picker is one flat `TOGGLEABLE_COLUMNS.map` in a 6-across grid — [inventory/page.tsx](<../../../frontend/app/(admin)/admin/inventory/page.tsx>). `CardDetailModal` solved the same problem for the same field set by grouping into Identity / Pricing / Acquisition / Notes / Flags ([CardDetailModal.tsx:53](../../../frontend/components/admin/shared/CardDetailModal.tsx#L53)). | Adding a `section` to every registry entry duplicates a grouping that already exists in the modal's `EDITABLE_FIELDS`, and the right move is probably to share ONE field-metadata source between the two rather than write a second one. That is a refactor across both surfaces, not a column task. | Finding "Cert #" means scanning 34 unsorted labels. Tolerable at this size, annoying beyond it — and the duplicated-vocabulary problem is the same one already logged under T2 row 2 and T5 row 3. Worth solving once, for all three. |
| **`vi.spyOn(window.localStorage, ...)` does not work under jsdom and passes against unpatched code.** jsdom backs `Storage` with a Proxy whose `defineProperty` trap writes a storage *entry* for any string key, so the spy stores an item literally called `getItem` and leaves the real method in place. Caught here because the storage-throws test went green before the try/catch existed; the fix is to spy on `Storage.prototype` ([admin-inventory-columns.test.ts](../../../frontend/lib/__tests__/admin-inventory-columns.test.ts)). | Not a code defect — a testing-technique trap, and the only two tests in the repo that touch `localStorage` are the ones T6 added. Documenting it in the test file is the whole fix. | Logged so the next person writing a storage test does not lose the same hour, and does not ship a green test that asserts nothing. |

## T8 — Admin set combobox over the whole catalog (done 2026-08-06)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| ~~**The combobox cannot be operated by keyboard alone.**~~ **RESOLVED (T-FINAL, 2026-08-06)** — ArrowUp/ArrowDown (wrapping), Enter to select, Escape to close, Tab to dismiss, plus `aria-activedescendant`. 6 tests. Both call sites benefit.  Options are chosen with `onMouseDown` and there is no arrow-key navigation, no Enter-to-select, no `aria-activedescendant` — [SetCombobox.tsx](../../../frontend/components/shared/SetCombobox.tsx). A keyboard-only admin can type to narrow the list and then has no way to pick anything from it. | Pre-existing in the public `FilterPanel`, and T8's brief for the extraction is that the public panel be **behaviourally unchanged** — its two combobox tests are the named regression guard. Adding keyboard handling inside the extraction would change the public control under cover of a refactor, which is how a regression gets attributed to the wrong task. | Now on an admin surface that is used far more heavily and more keyboard-first than the customer filter panel, so the same gap costs more than it did. This is the one item here worth fixing soon; it is ~20 lines in one shared component and both call sites get it at once. |
| **A retired or renamed set never leaves the registry.** `put_catalog_sets` is upsert-only — [dynamodb.py `put_catalog_sets`](../../../backend/src/merlins_collection/services/dynamodb.py) — so a set TCGdex drops or re-ids keeps its row forever, showing "0 owned". | Deliberate, and the alternative is worse: the sync degrades a failed `list_sets` to "no sets for this language", so a reconcile-and-delete pass would turn one language's upstream 503 into "every JA set vanished from the dropdown". Doing it safely means distinguishing "TCGdex dropped this set" from "TCGdex had a bad day", which is an owner-visible policy call, not a writer detail. | A dead set sits in a ~400-entry list reporting zero owned cards — indistinguishable from a real buying gap, which is the exact signal the list exists to give. Low frequency; TCGdex set ids are stable in practice. |
| **The registry only refreshes when someone presses "check for new sets".** `run_daily_sync` never calls `list_sets` (it is the depth/price pass), so `sync_new_sets` — the `/admin/market` button and the monthly schedule — is the sole writer besides the one-time backfill. | Adding a `list_sets` call to the daily job spends upstream requests every morning on data that changes when a set *releases*, roughly monthly. That cadence is the owner's call, and the button is already the documented way new sets arrive. | `set_name` and `card_count` go stale between runs; a set released since the last press is missing from the dropdown entirely. `owned_count` is computed per request and is never stale, so the number the owner actually opens the list for stays correct. |
| **`GET /admin/catalog/sets` reads the whole inventory on every call**, plus up to three chunked `BatchGetItem`s — [catalog.py `_owned_counts_by_set`](../../../backend/src/merlins_collection/routers/admin/catalog.py). Same shape as the `/admin/triage/counts` row already logged under T11. | Caching reintroduces exactly the staleness the live count avoids (a card bought this morning missing from its set's count), and T9's experience says do not add a cache before there is a measured problem. The list is fetched **once per page mount**, not per keystroke — which is itself pinned by a test. | Nothing at 266 items. Cost scales with inventory size × admin page loads, so this and the two triage rows are one problem to look at together if the admin panel ever feels slow — not three separate ones. |
| **`list_cards_by_set` reads GSI1, which is eventually consistent**, so a set whose cards landed moments earlier can read as empty — [dynamodb.py `list_cards_by_set`](../../../backend/src/merlins_collection/services/dynamodb.py#L888). The registry's `card_count` inherits this: a set misread as empty is walked, and its cards are counted as newly added even where the conditional write preserved an existing row. | Pre-existing and not caused by T8 — `new_sets` and `cards_added` have had the identical property since `sync_new_sets` was written, and the second layer of the no-overwrite guarantee (`preserve_priced=True`) is what makes the misread harmless for the card data itself. Fixing it means a strongly-consistent read, which GSIs do not offer at all. | An informational count can overstate a set's size for one sync cycle; the next run corrects it, since `held` is re-read every time. No card data is at risk. |
| **The backend still accepts `set_name`**, now unreachable from any UI — [admin/inventory.py `_filter_by_catalog`](../../../backend/src/merlins_collection/routers/admin/inventory.py). The combobox sends `set_id` exclusively. | Removing it breaks its existing tests and any Retool query built against it, for no gain — it is a working filter, just not the one the page uses now. | A future reader finds two set filters and has to work out which is live. The `set_id` path is the one with the combobox behind it; `set_name` is a substring match kept for compatibility. |
| **Neither the combobox nor the endpoint was opened in a browser.** All behaviour is pinned by vitest against a mocked `useAdminApi` and by pytest against moto; nothing rendered the real control against the real API. | Needs `npm run dev` plus a Cognito session in the admin group — the same constraint already logged under T4 row 1 and T7 row 4. | The two things jsdom models rather than exercises are the outside-click close (a real `mousedown` on `document`) and how ~400 options look inside a `max-h-60` scroll box. Worth one manual open-type-select pass before this ships. Note the registry is empty until `scripts/backfill_catalog_sets.py --execute` has been run, so the dropdown will be empty on the live table until then. |

## T2 — Condition facets emit LP+/LP- (done 2026-08-05)

| Finding | Why deferred | Impact if ignored |
|---|---|---|
| **`_parse_condition_query` now exists twice**, identically — [inventory.py:151](../../../backend/src/merlins_collection/routers/inventory.py#L151) and [admin/inventory.py:863](../../../backend/src/merlins_collection/routers/admin/inventory.py#L863). Both are thin 422 wrappers over the one shared `normalize_condition`. | Importing the admin one into the customer router would invert the dependency direction (customer → admin package, pulling `condition_pricing` and `locations` in on every customer request). The right home is a small shared `routers/_shared.py` or similar, which is a refactor touching admin — out of scope for a backend-only bugfix. | Low: the *parsing* cannot drift, since both delegate to `normalize_condition`. Only the 422 message text can, and it already differs slightly (the customer one builds its list from `_CONDITION_ORDER`, the admin one hardcodes the string). |
| **The condition vocabulary and its best-to-worst order are now declared in three places** — `_CONDITION_ORDER` ([inventory.py:118](../../../backend/src/merlins_collection/routers/inventory.py#L118)), `CONDITION_OPTIONS` ([constants.ts:8](../../../frontend/lib/constants.ts#L8)), and the `Condition` enum + `ConditionModifier` pair that only implies it. Nothing enforces agreement. | A cross-language constant needs either codegen or a contract test that fetches the list over the wire; both are bigger than T2 and neither is obviously worth it for a 7-element list that has not changed since Round 1. | A new grade (or a reordering) added to one and not the others gives the customer dropdown a different order from the admin editor's, or drops an option from the facet ranking so it silently sorts last. |
| **The `condition` param lost its OpenAPI enum.** It was typed `Condition`, so the generated schema advertised the five tiers; it is now `str` with `max_length=8`. | The enum was *wrong* — it never included `LP+`/`LP-`, which is the bug T2 fixed — and there is no combined-form enum to replace it with (adding one to `Condition` is forbidden; that was the Round 1 bug). Expressing it properly needs a `Literal[...]` or a pydantic annotated type. | The API docs no longer enumerate valid conditions for this param; a consumer reading the schema sees "any string ≤ 8 chars". The 422 response body lists the seven valid values, so it is discoverable at runtime rather than at design time. |

---

## T-FINAL triage pass (2026-08-06)

The owner triaged this ledger in one pass after T-FINAL's verification run.
**12 rows resolved** (struck through above). What was decided, and why:

| Decision | Rows |
|---|---|
| **Customer prices must include the condition multiplier** | T1 r1. The biggest correctness item on the list — a DMG card was displayed at ~6.7x the business's own valuation. Applied at enrichment so the tile, sort and price bound cannot diverge again, mirrored in `/inventory/summary` and MCP. Measured live: −18.5% on the customer-visible total |
| **`display_name_override` wins everywhere, client AND admin** | T10 r1, r2, r3. Four name resolvers now share one rule; the detail modal shows both name fields so either can be corrected |
| **All five "cheap fixes" applied** | T5 r4, T6 r2, T8 r1, T11 r7, T11 r8 |
| **Repo-wide lint debt cleared** | T9 r3 — `ruff check backend/src` is clean |

### Verified, not fixed

| Finding | Outcome |
|---|---|
| ECS memory vs the 93 MB cache (T9 r1) | **Not a risk.** Task is 2048 MB; the cache is ~4.5% of it |
| The catalog search fix, against live data (T9) | **Confirmed.** 11.2s -> **~20ms warm** (~500x). Substring matching intact: "surfing pikachu" -> 5 results |
| The 7 skipped backend tests | **Resolved 2026-08-07 — file deleted.** They were in `tests/scripts/test_language_recall.py`, dead because the production snapshot now holds 266 records rather than the 1266 their assertions were written against, and because `tcg_url` has been None by design since Phase 3.1. A later rewrite made them hermetic but left them a near-verbatim duplicate of `tests/services/test_card_text.py` (identical false-positive parametrize list). The four cases that were genuinely unique — `parse_language("(jp)")`, `parse_language(None)`/`("")`, an unrecognized stored `language`, and the lowercase `sv11b` set code — moved into `test_card_text.py`, which tests those functions from the module they live in |
| Triage badge vs list agreement (T11) | **Confirmed live.** Both report 28. Reason counts (21/13/17) exceed the total by design — items qualify severally |

### Still open, and still worth a decision

| Finding | Why it still matters |
|---|---|
| **Nothing has been seen in a browser** (T4 r1, T7 r4, T8 r7) | The smoke pass done here was API-level against live data. The date-input round-trip, the `<dialog>` confirm, outside-click close, and the 375px layout are still unexercised |
| **`/admin/shows` CRUD was not smoke-tested** | It writes to live business data; not done without the owner's say-so |
| **A re-import wipes every Triage decision** (T11 r1) | Unchanged. The generation sweep destroys `reviewed_at` along with the row |
| **`admin_update_item` still has no field allowlist** (T10 r4, T11 r2, T7 r3) | `card_id` is now validated, but `lineage_id`, `predecessor_item_id` and `consignment` remain reachable via `PUT` |
| **Mobile bottom nav shows 5 of 15 tabs** (T7 r5) | Shows and Triage are both unreachable on a phone |
| **The `catalog_set` registry is empty on the live table** | Confirmed live: `GET /admin/catalog/sets` returns 0. `scripts/backfill_catalog_sets.py --execute` has not been run, so the admin Set dropdown is empty |
